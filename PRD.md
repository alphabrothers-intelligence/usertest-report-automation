# PRD: 사용성테스트 결과보고서 자동생성 솔루션 (v1, SW/App형)

| | |
|---|---|
| 문서 버전 | v1.4 |
| 작성 기준 | 리바랩스 raw data(WALLA 응답 100건) 100% 검증 완료 |
| v1 빌드 범위 | **SW/App형만.** 장기 로드맵은 2.2절 참조(v1→v2 실물제품형→v3 통합) |
| 관련 문서 | 사용성테스트 결과보고서 표준스키마_SW앱형.pdf, 표준스키마_실물제품형.pdf, CLAUDE.md |
| 원본 레퍼런스 | 리바랩스 「캣독런」 사용성테스트 결과보고서 + raw data(100명, 59컬럼) |

### 개정 이력

**v1.0 → v1.1**: UI/배포 아키텍처를 확정했다. 초기에는 담당자용 폼 기반 검수 화면(Streamlit 가정)으로 설계했으나, **사용자가 채팅으로 자유롭게 대화하며 보고서를 생성하는 챗봇형 UI**로 방향을 바꾸고, **Vercel(Next.js) 단일 스택**에 배포하기로 확정했다. 이에 따라 3장(사용자 워크플로우), 4장(전체 아키텍처), 7장(Human-in-loop 설계), 10장(비기능요구사항)을 재작성했고, 6장의 프롬프트 내용 자체는 변경 없음(호출 방식만 `generateObject`+Zod 스키마 강제로 보강). 또한 "고품질·빠른 생성"이라는 목표를 실측으로 뒷받침하기 위해 **13장(토큰 사용 전략 검증 실험 계획)을 신규로 추가**했다.

**v1.1 → v1.2**: 제품 로드맵을 v1(SW/App형) → v2(실물제품형 추가) → v3("기본" 사용성테스트 보고서, 두 제품유형 통합) 3단계로 공식 확정했다(2.2절 신규). 기존에 "참고용, 채택 여부 미정"으로 남겨뒀던 부록 A.4의 조건부 섹션 통합 설계를 **v3의 확정 목표 설계**로 격상했다. v1 빌드 범위 자체는 변경 없음.

**v1.2 → v1.3**: 입력 방식을 raw data 단일 경로에서 **3단계 우선순위 체계**로 확장했다(2.1절, 5장). raw data(Tier 1)가 여전히 최우선이지만, 사람이 이미 통계·분류까지 정리한 **구조화된 요약본(Tier 2)**, 통계 없이 원문 텍스트만 있는 **자유서술 요약(Tier 3)**도 받을 수 있도록 범위를 넓혔다. Tier 2는 자유 형식 문서(AI가 구조를 추출)와 정해진 템플릿(결정론적 파싱) 두 가지 방식을 토글로 선택할 수 있게 했으며, 이를 위한 신규 추출 모듈과 체크포인트 0(추출결과 검수)을 추가했다(7.0절 신규). 또한 기업개요·주요기능 등 **제품 정보 선택 입력**을 추가하고(5.0절 신규), Vercel Blob **클라이언트 업로드** 방식으로 파일당 500MB까지 지원하도록 파일 업로드 아키텍처를 구체화했다(10장). 이 변경은 2.1절의 기존 "raw data 없이 생성 안 함" 원칙을 완전히 뒤집는 것이 아니라, **raw data가 없을 때 시스템이 통계를 임의로 지어내지 않는다는 원칙은 유지하면서** 입력 소스의 폭을 넓힌 것이다(Tier 3에서는 정량 섹션이 "정보 없음"으로 표시됨, 5.3절 참조).

**v1.3 → v1.4**: 실제 구현물을 담당자가 직접 사용자 입장에서 써보며 체크포인트 A/B 이전 단계의 UX 문제를 다수 발견했다 — 정량 통계가 카드에 다 담기지 않아 AI가 별도로 마크다운 텍스트를 재요약했는데 채팅 UI가 마크다운을 렌더링하지 않아 `**`·`|표|` 문법이 그대로 노출됨, 정성 분석처럼 비용·시간이 큰 단계로 들어가기 전에 "보고서를 어떤 목차/섹션으로 구성할지"에 대한 사용자 동의 절차가 없음, 문항이 많은 결과가 접기/펼치기 없이 통짜로 나와 한눈에 스캔하기 어려움. 이를 계기로 5 Whys로 1.1절 문제 정의를 다시 파고들었고, "반복 업무를 자동화한다"는 기존 정의에 **"자동화된 결과를 담당자가 얼마나 빠르고 편하게 확인·검수할 수 있는가"**를 명시적으로 결합했다(1.1절 개정, 근본원인 분석 표 신규 추가). 이에 따라 **3.2절(UX 설계 원칙, 신규)을 추가**하고, 3장 워크플로우 다이어그램에 **목차/섹션 구성 동의 단계**를 반영했으며, 7.2절에서 "고려한다"로 완곡하게 남겨뒀던 문항 단위 접기/펼치기 요구사항을 **필수 요구사항으로 격상**했다. 6장 프롬프트 본문·9장 fidelity 기준·1.3절 성공 기준 수치는 변경하지 않는다 — 이번 개정은 "무엇을 계산하는가"가 아니라 "그 결과를 어떻게 보여주는가"에 대한 것이다.

상세 배경은 `CLAUDE.md`의 "v2 변경 안내" 및 `docs/architecture_decision_log.md` 참고.

---

## 목차

0. 개정 이력 (위 참조)
1. 개요
2. 범위(Scope)
3. 사용자 워크플로우
4. 전체 아키텍처
5. 입력 데이터 스펙
6. 처리 파이프라인 및 프롬프트
7. Human-in-loop 설계
8. 출력 문서 조립 규칙
9. 품질 검증
10. 비기능 요구사항
11. 리스크 및 열린 질문
12. 개발 로드맵
13. 토큰 사용 전략 검증 실험 계획
- 부록 A. 제품유형별 섹션 구성 비교 (실물제품형 / SW·App형 / 통합본)
- 부록 B. 예상 토큰 비용 산정

---

## 1. 개요

### 1.1 문제 정의

**담당자가 raw data만 주면, 반복적인 통계 계산·응답 분류·초안 작성 부담 없이 최대한 빠르고 편하게 결과를 확인·검수할 수 있어야 한다 — 그래야 담당자가 본인만 할 수 있는 검증·판단(도메인 감각이 필요한 최종 확인)에 시간을 쓸 수 있다.**

이 정의는 표면적 증상("반복 업무가 귀찮다")에서 멈추지 않고, 담당자와의 5 Whys를 통해 근본원인까지 파고든 결과다(v1.4 개정).

| 단계 | 내용 |
|---|---|
| 표면 문제 | 담당자가 매번 새 고객사 raw data를 받을 때마다 통계 계산·응답 내용 추출/가공·도표 제작을 반복 수작업으로 수행 |
| Why 1 | 인력이 부족한 상태에서 이 반복 작업이 고객 미팅·신규 프로젝트 착수 같은 업무와 같은 인력을 두고 경쟁 → 병목 발생 |
| Why 2 | 병목이 납기 지연 리스크와 담당자 야근으로 드러남 |
| Why 3 | 야근으로 납기는 지켜왔지만, 서두르다 보고서 품질이 저하되고 잦은 퇴사로 이어짐 (실측 사례: 실제 발행 보고서에서 인접 문항 표준편차를 복사-붙여넣기하다 생긴 오타를 raw data 재계산으로 발견함 — CLAUDE.md "골든 테스트셋" 절 참고) |
| Why 4 | 퇴사 반복, 사업 확장 곤란, 특정 팀장 한 명에게 업무가 과중 집중 |
| **Why 5(근본원인)** | 이 업무는 **"초안 작성"은 위임 가능해도 "최종 검토·수정·검증"은 반드시 팀장 개인의 숙련된 판단을 거쳐야 하는 암묵지 영역**이라, 팀장의 시간이 반복적인 초안 작업 부담과 뒤섞여 있고 정작 팀장만 할 수 있는 검증에 온전히 집중하지 못한다 |

이 근본원인은 "자동화 자체"의 필요성은 뒷받침하지만, **"자동화된 결과를 얼마나 빠르고 편하게 확인·수정할 수 있는가"까지 설계 목표에 포함되지 않으면 절반만 푼 문제**라는 것을 v1.4 개정에서 명확히 했다(3.2절 UX 설계 원칙 참조). 즉 AI가 초안을 아무리 정확하게 채워도, 그 결과를 사람이 스캔하기 어렵거나(예: 접기/펼치기 없이 통짜로 나열됨) 무엇을 검수해야 하는지 바로 파악할 수 없다면, 팀장의 검수 부담은 형태만 바뀔 뿐 줄어들지 않는다.

raw data만 있으면 이 반복 작업의 상당 부분(통계 계산, 응답 분리·분류, 대표 인용문 선정, 인사이트 초안)이 규칙 기반 + LLM으로 재현 가능하다는 것은 리바랩스·케어클 두 사례의 raw data 실측 검증을 통해 확인했다.

### 1.2 목표

**raw data(xlsx/csv)를 업로드하면, 사용자가 채팅으로 자유롭게 대화하며 원본 사용성테스트 결과보고서(리바랩스 「캣독런」 보고서)와 최대한 동일한 구조·수준의 보고서를 자동 생성한다.** 별도의 폼 기반 화면 없이 채팅 인터페이스 하나로 업로드·진행상황 확인·검수·최종 발행까지 이어지며, Vercel(Next.js) 단일 스택에 배포한다.

"최대한 동일하다"는 표현을 막연하게 두지 않기 위해, 아래를 v1의 정량적 성공 기준으로 정의한다. 이 표는 **"결과 내용이 얼마나 정확한가"**를 측정하는 기준이고, **"그 결과를 담당자가 얼마나 빠르고 편하게 확인·검수하는가"**는 별도로 3.2절(UX 설계 원칙)에서 다룬다 — 1.1절 근본원인 분석에 따르면 이 둘은 서로 다른 문제이며 둘 다 풀려야 한다.

### 1.3 Fidelity(재현도) 성공 기준

| 항목 | 측정 방법 | v1 목표치 |
|---|---|---|
| 정량 통계(평균/표준편차/분포/%) | 실제 보고서 수치와의 오차 | **오차 0** (리바랩스는 raw data와 보고서가 완전 일치하는 레퍼런스이므로 필터링 이슈 없음. 9장 참조) |
| 카테고리 커버리지 | 실제 보고서에 등장한 `[카테고리명]`과 생성 결과 카테고리의 주제 일치율 | 기능당 70% 이상 |
| 극성 판정 일치율 | 원문 대조 가능한 문장 표본에 대해, 실제 보고서 배치 극성과 생성 결과 극성이 일치하는 비율 | 90% 이상 (긍정 vs 나머지 경계), 75% 이상 (부정 vs 중립 경계, 6.4절 참조) |
| 인용문 정확성 | 생성된 인용문이 raw data 원문과 완전히 일치(verbatim)하는 비율 | 100% (의역 금지, 프롬프트 레벨에서 강제) |
| 인사이트 문장 품질 | 사람 평가(정성 리뷰, 5점 척도) | 자동 측정 불가 영역이므로 별도 체크리스트 운용 (9.4절) |

---

## 2. 범위(Scope)

### 2.1 v1에 포함되는 것

- **제품유형**: SW/App형만. 리바랩스 raw data로 100% 검증된 파이프라인을 기준으로 한다.
- **입력 방식(v1.3 확장)**: 아래 3단계 우선순위 체계로 입력을 받는다. 상세 스펙은 5장 참조.

| Tier | 명칭 | 내용 | 시스템 처리 |
|---|---|---|---|
| **1** | raw data | WALLA 표준 59컬럼 xlsx | 정량·정성 전부 자동 생성 (기존 v1.2까지의 유일한 경로) |
| **2** | 구조화된 요약본 | 사람이 이미 통계·카테고리·인용까지 정리한 문서(자유 형식 또는 정해진 템플릿, 5.2절) | 서식 조립만 수행, 새로운 통계·분류를 계산하지 않음 |
| **3** | 자유서술 요약 | 통계·구조 없이 원문 텍스트만 있는 요약(5.3절) | 정성 섹션(카테고리·인용·인사이트)만 AI가 처리, 정량 섹션은 "정보 없음"으로 표시 |

  raw data가 최우선이며, 사용자가 raw data를 갖고 있지 않을 때 Tier 2·3을 대안으로 지원한다. 세 Tier 모두 공통으로 **AI가 raw data 없이 정량 통계·비율을 임의로 지어내지 않는다**는 원칙은 유지한다(9.1절, 5.3절 참조) — 이번 확장은 입력 소스의 폭을 넓힌 것이지, 이 원칙을 되돌린 것이 아니다.
- **제품 정보 입력(v1.3 신규)**: 기업개요·서비스요약·주요기능 등(5.0절)을 선택 입력으로 받는다. 건너뛰어도 보고서 생성이 진행되며, 입력하지 않은 필드는 "입력 필요"로 빈칸 유지한다. **AI가 임의로 추정해서 채우지 않는다**(종합전략제언과 동일한 원칙, 7.3절 참조).
- **입력 포맷**: Tier 1(raw data)은 WALLA 설문 응답 표준 컬럼 구조(리바랩스 기준으로 검증된 59컬럼 구조, 5장 참조) **하나만** 지원한다. 컬럼명이 다른 비표준 포맷에 대한 자동 매핑 기능은 v1 범위에서 제외한다.
- **raw data 처리 원칙**: 업로드된 행을 전량 그대로 집계 대상으로 삼는다. 원본 담당자가 최종 보고서 작성 전 일부 응답을 제외했는지 여부는 추정·재현하지 않는다(9.1절 참조).

### 2.2 제품 로드맵 (v1 → v2 → v3, v1.2 신규 확정)

본 PRD는 v1(SW/App형)만 빌드 대상으로 하지만, 장기 로드맵은 아래 3단계로 확정한다.

| 버전 | 착수 조건 | 범위 | 산출물 |
|---|---|---|---|
| **v1** (본 PRD) | 즉시 착수 | SW/App형만. 리바랩스 raw data로 100% 검증 | SW/App 전용 사용성테스트 보고서 생성 |
| **v2** | v1의 1.3절 fidelity 지표가 안정적으로 달성된 것을 확인한 후 | 실물제품형(케어클 기준) 추가. 별도 PRD로 작성 | 실물제품형 전용 사용성테스트 보고서 생성 |
| **v3** ("기본" 사용성테스트 보고서) | v1·v2가 각각 독립적으로 검증 완료된 후 | 두 제품유형을 하나의 시스템으로 통합 | raw data를 넣으면 제품유형을 판별해, 부록 A.4의 조건부 섹션 구성 로직에 따라 공통 섹션 + 제품유형별 섹션을 자동으로 합쳐 "기본" 보고서를 생성 |

**v3에서 풀어야 할 핵심 설계 과제** (지금은 확정하지 않고 v2 완료 후 논의):
- **제품유형 판별 방식**: 사용자가 채팅에서 명시("이건 실물제품이야")하게 할지, raw data 컬럼 구조로 자동 추론(예: 고객여정 관련 컬럼 존재 여부)할지. 자동 추론은 사용자 입력 부담을 줄이지만 오판 리스크가 있다.
- **입력 검증 모듈의 분기**: 5.1절 컬럼 스키마 검증이 현재는 WALLA(SW/App) 구조 하나만 보는데, v3에서는 제품유형별로 다른 스키마를 검증해야 한다.
- **공통 섹션과 조건부 섹션의 코드 재사용 구조**: 부록 A.4의 조건부 삽입 로직(`product_type` 플래그 기준)을 실제 구현으로 옮기는 작업.

### 2.3 v1에서 제외되는 것

- 실물제품형(고객여정 기반 경험평가, 구매전환율/재구매율 개선전략 등) — v2에서 추가
- 비표준 컬럼 포맷(Tier 1) 자동 매핑
- Tier 2·3 입력 없이 raw data도 없는 상태에서, AI가 정량 통계를 임의로 만들어내는 것 — 이 원칙은 v1.3에서도 그대로 유지(2.1절)
- 워드클라우드 생성 (현재 실제 보고서에서도 보류 중인 기능, 자리만 확보)
- 제품유형 통합(v3의 범위. 2.2절 참조)

---

## 3. 사용자 워크플로우

사용자는 별도의 업로드 화면이나 검수 화면을 거치지 않고, **채팅 대화 하나 안에서** 아래 흐름을 진행한다. 실제 발화는 자유롭게 이뤄지지만("이 raw data로 보고서 만들어줘", "이 항목은 중립으로 바꿔줘" 등), Claude가 tool calling으로 아래 순서를 내부적으로 보장한다. 체크포인트 단계는 별도 페이지가 아니라 **채팅 메시지 안에 렌더링되는 카드형 UI**로 제공된다(7장 참조).

```
① [선택] 제품 정보 입력 — 기업개요·서비스요약·주요기능 등(5.0절)
   - 건너뛰어도 다음 단계로 진행 가능. 입력 안 한 필드는 "입력 필요"로 빈칸 유지
   ↓
② 사용성테스트 데이터 입력 — 아래 중 하나를 채팅에 첨부 (2.1절 Tier 우선순위)
   ┌─ Tier 1: raw data(xlsx) ──────────────────────────┐
   │  [도구: validateInput] WALLA 표준 59컬럼 검증        │
   │  불일치 시 어떤 컬럼이 다른지 안내 후 중단            │
   └───────────────────────────────────────────────────┘
   ┌─ Tier 2: 구조화된 요약본 ──────────────────────────┐
   │  토글: 자유 형식 문서 / 정해진 템플릿 (5.2절)         │
   │  [도구: parseTemplateSummary] 또는                  │
   │  [도구: extractFreeformSummary] + [체크포인트 0]     │
   └───────────────────────────────────────────────────┘
   ┌─ Tier 3: 자유서술 요약(원문 텍스트만, 5.3절) ─────────┐
   │  정성 처리 파이프라인만 적용, 정량은 "정보 없음"       │
   └───────────────────────────────────────────────────┘
   ↓
③ [도구: computeQuantStats] 정량 통계 계산 (Tier 1만 해당. Tier 2는 이미 있는 값을
   그대로 사용, Tier 3는 생략) — 결과는 누락 없는 완전한 구조화 카드로 제시하고
   (3.2절), AI는 카드 내용을 별도 텍스트로 재요약하지 않는다
   ↓
④ [목차/섹션 구성 동의 — 인라인 카드, v1.4 신규, 3.2절] raw data를 읽고 파악한
   내용을 바탕으로 "보고서에 어떤 목차·섹션을 어떤 근거로 채울 예정인지" 계획을
   먼저 제시하고 사용자 동의를 받는다. 비용·시간이 큰 ⑤ 단계(정성 분석)는 이
   동의 이후에만 진행한다 — 사용자가 원치 않는 방향으로 몇 분간 API 비용을
   태우고 나서야 잘못을 알게 되는 상황을 막기 위함
   ↓
⑤ [도구: stage1SplitPolarity → stage2ClusterInsight, 14개 문항 병렬 처리]
   정성 응답 처리 (Tier 1·3 해당. Tier 2는 이미 분류된 내용을 그대로 사용).
   진행 상황을 채팅 메시지로 실시간 스트리밍
   (예: "펫과의 산책 분석 중... ✓ 완료(긍정 21건·부정 12건)"),
   결과는 문항 단위로 접기/펼치기 가능한 카드로 제시한다(3.2절, 7.2절)
   ↓
⑥ [도구: generateRecommendation] 제언 섹션 초안 생성 (헤지 워딩 적용)
   ↓
⑦ [체크포인트 A — 인라인 카드] 극성 판정 신뢰도 낮은 항목을 채팅 안에 제시
   - 사용자가 클릭으로 승인하거나 자연어로 수정 지시
   ↓
⑧ [체크포인트 B — 인라인 카드] 카테고리별 인사이트 문장 + 제언 문구 편집
   - 사용자가 직접 수정 (인사이트는 원래 관리자가 작성하는 영역이므로 필수 경유)
   ↓
⑨ [필수 입력] 종합 전략 제언 — 채팅으로 고객사 요청 맥락을 자연어로 전달
   - 사용자가 준 내용을 그대로 반영 (AI 생성 아님)
   ↓
⑩ [도구: assembleReport] 최종 PDF 조립, 채팅에 다운로드 링크 제공
```

**체크포인트 0(신규, 7.0절)**: Tier 2를 자유 형식 문서로 제공한 경우에만 발생한다. AI가 문서에서 추출한 통계·카테고리·인용문이 원문과 정확히 일치하는지, 섹션 매핑이 올바른지 사용자가 확인하는 단계다. Tier 1·정형 템플릿·Tier 3는 이 체크포인트를 거치지 않는다.

이 흐름은 사용자에게는 자유로운 대화처럼 보이지만, 시스템은 각 단계의 산출물을 DB(Vercel Postgres)에 저장해 두고 다음 채팅 턴에서 이어받는다(4.3절 참조) — 서버리스 함수는 요청마다 독립 실행되어 대화 맥락을 메모리에 들고 있을 수 없기 때문이다.

### 3.1 주요 사용자

- **1차 사용자**: 데이터전략본부 인텔리전스팀 담당자 (raw data 업로드, 체크포인트 A/B 검수, 종합전략제언 입력 — 전부 채팅 안에서 수행)
- **2차 사용자**: 팀장/검토자 (최종 발행 전 승인)

### 3.2 UX 설계 원칙 (v1.4 신규)

1.1절 근본원인 분석에서 확인했듯, 자동화가 실제로 검수 부담을 줄이려면 "정확한 결과를 만드는 것"만으로는 부족하고 "그 결과를 빠르고 편하게 확인·수정할 수 있는 것"까지 같이 갖춰야 한다. 담당자가 실제 구현물을 사용하며 겪은 마찰(아래 원칙별로 표기)을 근거로, 아래 3가지를 v1의 필수 요구사항으로 둔다.

| 원칙 | 내용 | 실사용 중 발견된 문제 |
|---|---|---|
| **완전한 구조화 출력, 텍스트 재요약 금지** | 도구 실행 결과는 해당 데이터의 모든 필드를 빠짐없이 보여주는 카드형 UI로 제공한다. AI는 카드가 이미 보여주는 내용을 채팅 텍스트로 다시 요약·설명하지 않는다 | 정량 통계 카드가 일부 필드만 보여줘서, AI가 누락분을 채우려고 별도 마크다운 텍스트를 썼고, 채팅 UI는 마크다운을 렌더링하지 않아 `**`·`\|표\|` 문법이 그대로 노출됨 |
| **비용이 큰 단계 진입 전 계획 동의** | 정성 분석처럼 API 호출 수·소요 시간이 큰 단계에 들어가기 전에는, 무엇을 어떻게 처리할 예정인지 먼저 제시하고 명시적 동의를 받은 뒤에만 실행한다(3장 워크플로우 ④) | 목차/섹션 구성에 대한 사전 확인 없이 바로 14문항 분석이 진행돼서, 사용자가 원치 않는 방향이었어도 몇 분(실측 900초+)이 지나야 알 수 있었음 |
| **항목이 많은 결과는 접기/펼치기 단위로 제공** | 문항·카테고리처럼 개수가 많은 결과는 통짜 목록이 아니라 단위별로 접고 펼 수 있는 카드로 제공해, 무엇을 검수해야 하는지 한눈에 스캔할 수 있게 한다 | 정성 분석 결과 14문항이 접기/펼치기 없이 한 번에 나열돼서 검수 대상을 파악하기 어려웠음. 7.2절이 이미 "고려한다"로 언급했던 요구사항을 v1.4에서 필수로 격상함 |

이 원칙들은 6장 프롬프트 본문이나 9장 fidelity 기준(1.3절)을 바꾸는 것이 아니라, **이미 정확하게 계산·생성된 결과를 담당자가 어떻게 받아보는가**에 대한 요구사항이다.

---

## 4. 전체 아키텍처

**Next.js(Vercel) 애플리케이션 하나**로 프런트엔드·백엔드를 통합한다(별도 서버 없음). 사용자가 채팅으로 보내는 메시지는 AI SDK 기반 채팅 엔드포인트로 들어가고, Claude가 tool calling으로 아래 도구들을 필요에 따라 호출한다.

```
┌──────────────┐     ┌───────────────────┐     ┌─────────────────────────────┐
│  raw data    │ ──▶ │ validateInput      │ ──▶ │  정규화 데이터 (내부 스키마)   │
│  (xlsx, 채팅   │     │ (컬럼구조 대조)     │     │  {question_id, respondent_id,│
│   첨부)       │     └───────────────────┘     │   score, reason_text, ...}   │
└──────────────┘                                └───────────────┬───────────────┘
                                                                 │
                        ┌────────────────────────────────────────┼────────────────────────────┐
                        ▼                                        ▼                             ▼
              ┌───────────────────┐                  ┌───────────────────────┐    ┌─────────────────────┐
              │ computeQuantStats  │                  │ stage1SplitPolarity     │    │ (순위/NPS는          │
              │ - 평균/표준편차      │                  │  → stage2ClusterInsight │    │  computeQuantStats   │
              │ - 분포/6.8 상대중요도│                  │  (14개 문항, p-limit로  │    │  안에 포함)          │
              │ - 규칙기반, LLM불필요 │                  │   동시 5~10개 병렬처리) │    │                     │
              └─────────┬─────────┘                  └───────────┬────────────┘    └──────────┬──────────┘
                        │                                        │                             │
                        └────────────────────────┬───────────────┴─────────────────────────────┘
                                                  ▼
                                     ┌─────────────────────────┐
                                     │  confidenceScoring         │
                                     │  (극성 판정 신뢰도, 6.4절)   │
                                     └────────────┬─────────────┘
                                                  ▼
                        ┌─────────────────────────────────────────────────┐
                        │  체크포인트 A/B (채팅 인라인 카드)                    │
                        │  — 사용자 응답 대기 동안 DB(Vercel Postgres)에 상태 저장│
                        └────────────────────────┬──────────────────────────┘
                                                  ▼
                                     ┌─────────────────────────┐
                                     │ generateRecommendation /   │
                                     │ generateSummary (LLM,       │
                                     │ 헤지워딩 강제)              │
                                     └────────────┬─────────────┘
                                                  ▼
                                     ┌─────────────────────────┐
                                     │  assembleReport            │
                                     │  (react-pdf, 3종세트 조립)  │
                                     └─────────────────────────┘
```

### 4.1 설계 원칙 (기존)

- 정량 처리는 LLM을 쓰지 않는다(재현성·비용·속도 모두 규칙 기반이 유리하고, 원본과의 오차 0을 목표로 하는 항목이므로 결정론적 계산이 필수). LLM은 정성 응답 처리와 제언 문구 생성에만 사용한다.

### 4.2 병렬 처리 원칙 (v1.1 신규 — 필수 요구사항)

문항 14개 × Stage1/Stage2 = 28회 호출을 **절대 순차로 처리하지 않는다.** 문항 간 Stage1은 서로 완전히 독립적이므로 동시에 처리하고, Stage2도 각 문항의 Stage1 결과가 나온 뒤 문항 간에는 병렬로 처리한다. 동시 호출 수는 Claude API 레이트리밋을 고려해 5~10개로 제한한다(`p-limit` 등으로 구현). 이 원칙을 지키지 않으면 Vercel Fluid Compute의 함수 실행시간(기본 300초)을 초과할 위험이 있다. 상세 근거는 10장 참조.

### 4.3 상태 저장 원칙 (v1.1 신규 — 필수 요구사항)

채팅은 턴 기반이라 각 사용자 메시지가 독립된 서버리스 함수 실행으로 처리된다. 이전 턴에서 처리한 raw data 파싱 결과, Stage1/Stage2 산출물, 체크포인트 승인 상태를 메모리에 들고 있다가 다음 요청에서 재사용할 수 없다. 이 모든 중간 상태는 **Vercel Postgres**에 저장하고, 다음 채팅 턴에서 조회해 이어간다.

### 4.4 구조화된 출력 (v1.1 신규)

Stage1/Stage2의 JSON 출력은 프롬프트 지시(6.2·6.3절의 "JSON만 출력하세요")에만 의존하지 않고, AI SDK의 `generateObject` + Zod 스키마로 구조를 강제한다. 6장의 프롬프트 본문 자체는 변경하지 않으며, 이는 출력 파싱 방식을 스키마 검증 방식으로 보강하는 구현 세부사항이다.

---

## 5. 입력 데이터 스펙

### 5.0 제품 정보 입력 스펙 (선택, v1.3 신규)

Ⅰ장(개요) 섹션에 필요한 정보다. raw data(Tier 1~3 어느 것에도)에는 원천적으로 존재하지 않는 정보이므로, 채팅 흐름의 첫 단계에서 별도 입력 폼으로 받는다(3장 ①).

| 필드 | 설명 | 필수 여부 |
|---|---|---|
| 기업명 / 홈페이지 / 대표자 / 업무담당자 | 기업 개요 | 선택 |
| 서비스명 / 서비스요약 / 사업영역 / 산업분야 / 운영환경 / 사업화단계 | 제품·서비스 개요 | 선택 |
| 주요 기능 목록 | 기능명 + 설명 + 이미지(선택, 500MB 이하, 10장 참조) 반복 가능한 리스트 | 선택 |

- 전체가 선택 입력이며, 건너뛰어도 다음 단계(사용성테스트 데이터 입력)로 진행한다.
- 입력하지 않은 필드는 보고서에서 **"입력 필요"로 빈칸 유지**한다. **AI가 사업영역·산업분야 등을 raw data나 문맥에서 임의로 추정해 채우지 않는다** — 종합전략제언(7.3절)과 동일한 원칙이다.

### 5.1 Tier 1: raw data 입력 스펙 (WALLA 표준, SW/App형)

리바랩스 raw data(100건)로 100% 검증된 컬럼 구조다. 아래 구조와 일치하지 않는 파일은 Tier 1로 처리하지 않는다(Tier 2·3으로 재시도 가능, 5.2·5.3절 참조).

| 컬럼 인덱스 | 컬럼명 | 타입 | 대응 섹션 | 비고 |
|---|---|---|---|---|
| 0 | 리크루팅 | 문자열(세그먼트 코드) | Ⅱ | 세그먼트 구분용 |
| 1 | 나이 | 정수 | Ⅱ | |
| 2 | 성별 | 범주형 | Ⅱ, Ⅶ(교차분석 축) | |
| 3 | 운영체제 | 범주형 | Ⅱ | |
| 4 | 하루 평균 걷는 시간 | 범주형 | Ⅱ | |
| 5 | 일주일 기준 산책 빈도 | 범주형 | Ⅱ | |
| 6, 8, 10, 12, 14, 16 | `'기능명' 기능 만족도` × 6 | 정수 0~10 | Ⅲ | 기능 6개 고정(리바랩스 기준. 프로젝트마다 기능 리스트는 가변) |
| 7, 9, 11, 13, 15, 17 | 이유 / 이유.1~5 | 서술형 텍스트 | Ⅲ | 정성 처리 대상 |
| 18~23 | 1위~6위 | 범주형(기능명) | Ⅳ | 순위 응답 |
| 24 | 캣독런 외 걷기 서비스 경험 유무 | 범주형(분기) | Ⅱ 하위 | |
| 25 | 사용해본 걷기 서비스 | 서술형 | Ⅱ 하위 | 24가 "있음"일 때만 값 존재 |
| 26 | 해당 서비스 만족도 | 정수 0~10 | Ⅱ 하위 | 〃 |
| 27 | 이유.6 | 서술형 | Ⅱ 하위 | 〃 |
| 28 | 가장 영향을 미칠 수 있는 핵심 요인 | 범주형(단일선택) | Ⅳ | |
| 29 | 이유.7 | 서술형 | Ⅳ | 핵심요인 해석 참고용 |
| 30, 32, 34, 36 | 기능적/심미적/경제적/사회공공적 가치 만족도 | 정수 0~10 | Ⅴ | |
| 31, 33, 35, 37 | 이유.8~11 | 서술형 | Ⅴ | 정성 처리 대상 |
| 38, 42, 46, 50 | 실용성1~4 | 정수 0~10 | Ⅵ | 양극형 척도 |
| 39, 43, 47, 51 | 이유.12,14,16,18 | 서술형 | — | **raw data엔 존재하나 실제 보고서는 정량만 노출. 정성 처리 파이프라인 미적용 (6.7절 참조)** |
| 40, 44, 48, 52 | 즐거움1~4 | 정수 0~10 | Ⅵ | 양극형 척도 |
| 41, 45, 49, 53 | 이유.13,15,17,19 | 서술형 | — | 상동 |
| 54 | 전반적인 만족도 | 정수 0~10 | Ⅷ | |
| 55 | 이유.20 | 서술형 | Ⅷ | 정성 처리 대상 |
| 56 | NPS | 정수 0~10 | Ⅷ | |
| 57 | 이유.21 | 서술형 | Ⅷ | 정성 처리 대상 |
| 58 | 개선 아이디어 제안 | 서술형(자유) | Ⅷ | 정성 처리 대상 |

#### 입력 검증 규칙

- 컬럼 개수(59개) 및 헤더 패턴(정규식: `이유(\.\d+)?` 등)이 일치하지 않으면 처리를 중단하고 어떤 컬럼이 기대와 다른지 구체적으로 표시한다.
- 응답 행 수에는 제약을 두지 않는다(리바랩스는 100건이나, 시스템은 임의 n에 대해 동작해야 함).
- 결측값(예: 24번 컬럼이 "아니요"일 때 25~27번이 결측)은 정상 케이스로 처리하고 에러로 취급하지 않는다.

### 5.2 Tier 2: 구조화된 요약본 입력 스펙 (v1.3 신규)

사람이 이미 통계·카테고리·인용·인사이트까지 정리한 문서다. 시스템의 역할은 **새로운 값을 계산하는 것이 아니라, 기존 값을 표준 섹션 형식(8장 3종세트)으로 조립하는 것**이다. 토글로 두 가지 입력 방식 중 하나를 선택한다.

**방식 A — 정해진 템플릿(권장, 결정론적 파싱)**

시스템이 제공하는 다운로드 템플릿(xlsx)에 맞춰 작성한다. 초안 구조(상세 컬럼 스펙은 Phase 1에서 확정):

| 시트 | 내용 |
|---|---|
| 기본정보 | 응답자 수, 조사기간 등 |
| 정량통계 | 문항명 \| 평균 \| 표준편차 \| (순위형 문항의) 순위별 비율 |
| 정성분석 | 문항명 \| 극성(긍정/부정/중립) \| 카테고리명 \| 인용문 \| 인사이트 |
| 제언 | 섹션명 \| 제언 내용 |

- raw data(5.1절)와 마찬가지로 컬럼 구조 검증 후 결정론적으로 파싱한다. `parseTemplateSummary` 도구가 처리하며, 이 경로는 체크포인트 0(7.0절)을 거치지 않는다(이미 구조가 명확하므로).

**방식 B — 자유 형식 문서 (AI 추출, `extractFreeformSummary` 도구)**

워드/PDF/텍스트 등 자유 형식 문서에서 AI가 통계·카테고리·인용문·인사이트를 찾아 표준 내부 스키마(Stage2 출력과 동일한 형태)로 추출한다.

- **절대 규칙**: 추출은 오직 "이미 문서에 쓰여 있는 값을 찾아 옮기는 것"이며, 새로 계산·재구성·의역하지 않는다. 문서에 "6.35점"이라고 쓰여 있으면 그대로 6.35를 사용하고, 반올림하거나 다른 값으로 대체하지 않는다(6.2절 verbatim 원칙과 동일한 정신).
- 문서 구조가 표준 섹션(Ⅰ~Ⅸ)과 다르거나 일부 섹션이 문서에 없는 경우, 있는 것만 추출하고 없는 섹션은 5.3절과 동일하게 "정보 없음"으로 남긴다.
- 섹션 매핑이 애매하거나 추출 신뢰도가 낮은 항목은 낮은 신뢰도로 플래그한다.
- 추출 결과는 **체크포인트 0(7.0절)에서 반드시 사용자 검수를 거친 뒤에만** 다음 단계로 넘어간다. Tier 2 중 유일하게 AI의 해석이 개입하는 경로이므로, 세 가지 입력 경로(Tier 1·2방식A·3) 중 가장 오류 위험이 높다.

### 5.3 Tier 3: 자유서술 요약 입력 스펙 (v1.3 신규)

통계·구조 없이 원문 텍스트(응답자 코멘트 모음, 메모 등)만 있는 경우다.

- 정성 처리 파이프라인(6.2·6.3절 Stage1·Stage2)은 Tier 1과 동일하게 적용한다 — 극성 판정·카테고리화는 텍스트 내용만으로 가능한 작업이기 때문이다(6.2절에서 이미 확인했듯 텍스트가 판정의 주된 근거).
- **정량 섹션(평균·표준편차·%, 상대중요도, NPS)은 계산하지 않고 "정보 없음"으로 명시 표시한다.** 점수 데이터 자체가 없으므로 AI가 추정으로 채우면 안 된다(2.1절 원칙).
- 산출되는 보고서는 완전한 형태가 아니라 "정성 섹션만 채워진 보고서"가 되는 것이 정상 동작이다.

---

## 6. 처리 파이프라인 및 프롬프트

### 6.1 파이프라인 개요

정성 응답이 있는 문항은 총 14개다(위 표에서 "정성 처리 대상"으로 표시된 것: 기능 6개 + 4대가치 4개 + 유사서비스만족도 1 + 전반적만족도 1 + NPS 1 + 개선아이디어 1). 각 문항마다 2단계 LLM 호출을 거친다.

| Stage | 목적 | 호출 단위 | 모델 권장 |
|---|---|---|---|
| Stage 1 | 문장/절 분리 + 극성 판정 | 문항 1개당 1콜(해당 문항의 전체 응답자 데이터를 한번에) | Claude Sonnet |
| Stage 2 | 극성별 카테고리 클러스터링 + 대표인용 선정 + 인사이트 생성 | 문항 1개당 1콜 (Stage1 출력을 입력으로) | Claude Sonnet |

14개 문항 × 2단계 = **28회 호출**로 정성 파이프라인이 구성된다. (개선아이디어는 특성상 극성 구분 없이 카테고리화만 하므로 Stage1을 단순화한 변형을 사용 — 6.6절 참조)

> **실행 방식**: 이 28회 호출은 순차가 아니라 **반드시 병렬로 실행한다**(4.2절 병렬 처리 원칙). 문항 간 Stage1은 서로 완전히 독립적이며, Stage2도 문항 간에는 독립적이다. 동시 호출 수는 `p-limit` 등으로 5~10개로 제한한다.

### 6.2 Stage 1 프롬프트 — 문장/절 분리 + 극성 판정

```
[SYSTEM]
당신은 사용성테스트 주관식 응답을 분석하는 리서치 애널리스트입니다.
아래 규칙에 따라 응답자들의 (점수, 이유) 데이터를 처리하세요.

# 작업 순서
1. 각 응답자의 "이유" 텍스트를 의미 단위(절 또는 문장)로 분리합니다.
   한 응답 안에 서로 다른 주제나 서로 다른 극성(긍정/부정/중립)의 내용이
   섞여 있으면 반드시 분리하세요. 하나의 응답이 여러 절로 나뉘어 서로 다른
   카테고리·극성에 배치되는 것이 정상입니다.
2. 분리된 각 단위에 극성을 판정합니다.
3. 판정 근거를 한 줄로 남깁니다.

# 극성 판정 기준 (가장 중요한 규칙)
- positive: 만족감, 효과 체감, 편리함 등을 진술
- negative: 문장 안에 **구체적인 부정적 결과**가 명시됨
  (예: 시간 손실, 오작동, 신체적·물리적 불편, 명시적 기능 결함,
  특정 상황에서 실제로 겪은 지장)
- neutral: 결함에 대한 구체적 서술 없이 개선 제안·선호·정보요청형
  의견만 존재. "불편하다", "아쉽다" 같은 표현이 있어도 구체적으로
  어떤 손해·지장이 발생했는지 서술이 없으면 neutral로 분류할 수 있습니다.

**표면적 어휘만으로 판단하지 마세요.** "불편하다"라는 단어가 있다고
자동으로 negative가 아닙니다. 문장이 서술하는 내용의 성격
(실제로 발생한 문제 vs 단순 선호/제안/비교)을 보고 판단하세요.
점수(score)는 텍스트만으로 판단이 정말 애매할 때만 보조적으로
참고하고, 텍스트 내용이 명확하면 점수와 무관하게 텍스트 기준으로
판정하세요.

# Few-shot 예시

## 예시 1 — 순수 긍정
입력: score=7, reason="위 기능에 대한 불편함은 못느낌"
출력: [{"clause": "위 기능에 대한 불편함은 못느낌", "polarity": "positive",
        "rationale": "결함 없음을 명시적으로 진술"}]

## 예시 2 — 동일 점수, 반대 극성 (점수만으로 판단하면 안 되는 이유)
입력: score=7, reason="그립감이 떨어지며 각도를 조정 잘 해야하는 불편함"
출력: [{"clause": "그립감이 떨어지며 각도를 조정 잘 해야하는 불편함",
        "polarity": "negative",
        "rationale": "신체적 불편(그립감 저하)이라는 구체적 결과가 명시됨"}]

## 예시 3 — 표면적으로 비슷한 "불편하다" 표현이 다르게 분류되는 경계 사례
입력: score=7, reason="Body Shot 할때 부위가 넓어 일시정지없이 사용되있으면
합니다 중간에 멈추니까 불편하고 시간이 너무 걸려요 / 페이스도 일시정지
없이 게속하는게 좋을것 같아요 / 내가 원하는곳에 조금더 하고싶은데
모드변경을 다시 제설정해야 되어 불편합니다"
출력: [
  {"clause": "Body Shot 할때 부위가 넓어 일시정지없이 사용되있으면 합니다
             중간에 멈추니까 불편하고 시간이 너무 걸려요",
   "polarity": "negative",
   "rationale": "시간 손실이라는 구체적 결과가 명시됨"},
  {"clause": "페이스도 일시정지 없이 게속하는게 좋을것 같아요",
   "polarity": "neutral",
   "rationale": "구체적 결과 서술 없이 기능 개선 선호만 표현"},
  {"clause": "내가 원하는곳에 조금더 하고싶은데 모드변경을 다시
             제설정해야 되어 불편합니다",
   "polarity": "neutral",
   "rationale": "'불편하다'는 표현이 있으나 구체적 손실이 없고
                재설정 절차에 대한 선호 표현에 가까움"}
]

## 예시 4 — 한 응답이 3개 극성에 걸쳐 분리되는 사례
입력: score=4, reason="시간, 거리 등이 나와서 정말 운동 앱 같은 느낌이다.
상자를 먹을 때 어느정도까지 가까이 가야 먹을 수 있는지 확실하지 않다.
제한 속도가 어느정도인진 모르겠으나, 타겜에 비해 좀 느슨한 편인 거 같다."
출력: [
  {"clause": "시간, 거리 등이 나와서 정말 운동 앱 같은 느낌이다",
   "polarity": "positive", "rationale": "운동 효과 체감을 긍정적으로 진술"},
  {"clause": "상자를 먹을 때 어느정도까지 가까이 가야 먹을 수 있는지
             확실하지 않다",
   "polarity": "negative",
   "rationale": "상호작용 기준 불명확이라는 구체적 사용상 문제"},
  {"clause": "제한 속도가 어느정도인진 모르겠으나, 타겜에 비해
             좀 느슨한 편인 거 같다",
   "polarity": "neutral",
   "rationale": "타 서비스와의 단순 비교·관찰이며 본인이 겪은
                구체적 불이익 서술이 없음"}
]

# 출력 형식 (JSON만 출력하세요. 다른 설명 텍스트를 앞뒤에 붙이지 마세요.)
{
  "results": [
    {
      "respondent_id": <정수>,
      "score": <정수>,
      "clauses": [
        {"clause": "<원문 그대로>", "polarity": "positive|negative|neutral",
         "rationale": "<판정근거 한 줄>"}
      ]
    }
  ]
}

# 절대 규칙
- clause 필드는 원문을 그대로 복사하세요. 절대 의역·축약·맞춤법 교정을
  하지 마세요. (이 값은 보고서에 실제 고객 인용문으로 그대로 실립니다.)
- 의미 있는 내용이 없는 응답("없음", "특별히 없음", 공백 등)은
  clauses를 빈 배열 []로 두세요.
- 오탈자나 비문이 있어도 원문 그대로 두세요.

[USER]
다음은 '{question_label}' 문항에 대한 응답자 {n}명의 (점수, 이유) 데이터입니다.

{raw_data_jsonl}
```

**입력 데이터 형식(raw_data_jsonl)**: `{"respondent_id": 3, "score": 4, "reason": "..."}` 형태의 JSONL을 그대로 concat하여 삽입.

### 6.3 Stage 2 프롬프트 — 카테고리 클러스터링 + 대표인용 + 인사이트 초안

> **분류 수정**: 카테고리 라벨링·인용문 선정(label, quotes)은 원문에서 규칙적으로 도출되는 작업이라 Tier 1(완전 자동생성)로 둔다. 그러나 `insight` 필드(→ 화살표로 시작하는 한 줄 요약)는 실제 운영 프로세스에서 **관리자가 직접 작성하는 영역**이다. 따라서 이 프롬프트가 생성하는 insight는 **Tier 2(AI 초안 + 사용자 필수편집)**로 취급하고, label·quotes만 자동 확정한 뒤 insight는 체크포인트 B로 전달해 검수를 거친 뒤에만 최종 문서에 반영한다(7.2절 참조). 8장 조립 규칙도 이 기준으로 수정했다.

```
[SYSTEM]
당신은 사용성테스트 결과보고서를 작성하는 애널리스트입니다.
Stage1에서 극성별로 분류된 응답 절(clause) 목록을 받아, 실제 발행되는
보고서와 동일한 형식으로 카테고리화합니다.

# 작업 순서
1. 같은 극성 내에서, 유사한 주제·맥락을 가진 clause들을 그룹핑합니다.
2. 각 그룹에 대괄호 카테고리명을 붙입니다. (예: "GPS 및 걸음 수 측정
   부정확성 문제")
3. 각 카테고리에서 가장 대표성 있고 구체적인 인용문을 2~4개 선택합니다.
   (해당 극성의 clause를 전부 인용하지 않습니다. 대표성 있는 것만 고릅니다.)
4. 각 카테고리 마지막에 인사이트 한 줄을 씁니다. 인사이트는 관찰·시사점
   톤으로 쓰고("~개선 필요", "~니즈 확인", "~정합성 개선 필요"),
   화살표 기호는 붙이지 마세요(조립 단계에서 자동으로 붙습니다).

# 카테고리 세분화 지침 (중요)
큰 대분류(2~3개)로 뭉뚱그리지 마세요. 실제 언급된 구체적 대상·맥락
단위로 세분화합니다. 예를 들어 "지도 조작 문제"와 "GPS 정확도 문제"는
원인이 다르므로 별도 카테고리로 분리해야 합니다. 실제 보고서는 한
극성 안에 보통 4~9개의 세부 카테고리를 사용합니다. clause 개수가
너무 적어(1~2개) 별도 카테고리로 보기 어려운 경우는 유사 카테고리에
합치거나 "기타" 성격 카테고리로 묶어도 됩니다.

# Few-shot 예시
입력(negative clause 목록 일부):
[
  {"respondent_id": 11, "clause": "gps가 부정확해서 걸은 길이나
   걸음수가 제대로 체크 되지 않아요"},
  {"respondent_id": 16, "clause": "경로가 지도상 도로를 따라 찍히지
   않고 건물을 뚫고 찍히거나 가끔 아예 다른 길로 표시되어 아쉬웠다"},
  {"respondent_id": 40, "clause": "실제 걸음 수와 게임에서 측정하는
   걸음 수에 많은 차이가 있었다"}
]
출력:
{
  "label": "GPS 및 걸음 수 측정 부정확성 문제",
  "quotes": [
    "gps가 부정확해서 걸은 길이나 걸음수가 제대로 체크 되지 않아요",
    "경로가 지도상 도로를 따라 찍히지 않고 건물을 뚫고 찍히거나
     가끔 아예 다른 길로 표시되어 아쉬웠다"
  ],
  "insight": "GPS 정확도 및 걸음 수 측정 정확도 개선 필요"
}

# 출력 형식 (JSON만 출력)
{
  "polarity": "positive|negative|neutral",
  "total_clause_count": <정수, 입력으로 받은 clause 총 개수>,
  "categories": [
    {"label": "<카테고리명>", "clause_count": <정수, 이 카테고리에 속하는
     전체 clause 개수 — quotes에 포함 안 된 것도 포함>,
     "quotes": ["<원문1>", "<원문2>"], "insight": "<인사이트 문장>"}
  ]
}

# 절대 규칙
- quotes는 입력으로 받은 clause 원문 중에서만 verbatim으로 선택하세요.
  새로 문장을 만들거나 여러 clause를 합쳐 재구성하지 마세요.
- clause_count의 합은 total_clause_count와 일치해야 합니다
  (모든 clause가 어딘가의 카테고리에 속해야 합니다).

[USER]
'{question_label}' 문항의 {polarity_kr} 응답 {m}건입니다.

{stage1_clauses_filtered_by_polarity_jsonl}
```

**% 계산(조립 모듈에서 규칙 기반으로 처리, LLM 미사용)**:
`percentage = polarity별 total_clause_count / (긍정+부정+중립 total_clause_count 합) × 100`

### 6.4 극성 판정 신뢰도 스코어링 (체크포인트 A 연계)

Stage 1 출력에 아래 규칙(LLM 재호출 없이 규칙 기반)으로 신뢰도 플래그를 추가한다.

| 조건 | 신뢰도 | 처리 |
|---|---|---|
| polarity="positive" | 높음 | 자동 승인 (전수 검수 제외) |
| polarity="negative" 또는 "neutral" 이면서 rationale에 "불편", "아쉽" 등 감정어가 포함 | **낮음** | 체크포인트 A 검수 대상 |
| polarity="negative" 또는 "neutral" 이면서 감정어 없이 명확한 결과/제안 서술 | 중간 | 검수 대상이나 우선순위 낮음 |

이 규칙은 6.2절의 실증 사례(동일하게 "불편"이 포함된 문장이 negative/neutral로 갈렸던 사례)에서 착안한 1차 휴리스틱이며, 운영 데이터가 쌓이면 정밀화가 필요하다(11장 참조).

### 6.5 제언 생성 프롬프트 — 헤지 워딩 강제 (Tier 2: AI초안 + 사용자 필수편집)

```
[SYSTEM]
당신은 사용성테스트 결과보고서의 "제언" 섹션을 작성하는 애널리스트입니다.

# 작성 규칙 (반드시 준수)
1. 먼저 데이터에 근거한 사실을 객관적으로 정리합니다.
   (예: "펫 레이싱 기능은 상대중요도 -2.27, 만족도 6.04점으로
   6개 기능 중 하위권입니다")
2. 그 다음 해석을 덧붙이되, 반드시 헤지(hedge) 워딩을 사용합니다.

   금지 표현 (사용하지 마세요):
   - "~이다" / "~다" 로 끝나는 단정적 서술
   - "~로 판단된다" / "~로 판단됨"
   - "~인 요소이다" / "~때문이다"

   필수 표현 (반드시 이 중 하나로 해석 문장을 마무리하세요):
   - "~로 해석할 수 있음"
   - "~로 볼 수 있음"
   - "~일 가능성이 있음"
   - "~로 보임"

3. 이 텍스트는 초안이며, 담당자가 검수 후 직접 수정합니다.
   확정적으로 들리는 표현을 피하세요.

# 예시
나쁜 예: "펫 레이싱 기능은 핵심 기능이 아니므로 개발 우선순위에서
         제외해야 한다."
좋은 예: "펫 레이싱 기능은 상대중요도(-2.27)와 만족도(6.04점) 모두
         낮은 편으로, 핵심 성과 거리가 있어 단기 개선 우선순위가
         낮은 기능으로 해석할 수 있습니다."

[USER]
아래는 '{섹션명}' 관련 정량 분석 결과입니다.
이 결과를 바탕으로 제언 문단을 작성하세요. (3~5문장)

{데이터 요약 JSON}
```

이 프롬프트는 아래 두 곳에 재사용된다.
- Ⅳ. 핵심구매요소 해석 서술
- Ⅸ. 종합결과 및 제언 → 개발 우선순위 제언, 기능개선제안(As-is→To-be)

### 6.6 개선아이디어(58번 컬럼) 처리 — 변형 파이프라인

개선아이디어는 점수가 없는 자유서술 단일 문항이라 극성 판정이 필요 없다. Stage1을 "문장분리만" 수행하는 축약 버전으로 변형하고, Stage2(카테고리화+인용+인사이트)는 동일하게 적용한다.

```
[SYSTEM 변경 부분만 발췌]
이 문항은 점수 없이 자유서술로 개선 아이디어를 받는 질문입니다.
극성 판정 없이, 응답을 의미 단위로 분리하고 주제별로만 표시하세요.
(polarity 필드는 생략합니다)
```

### 6.7 UX품질평가(실용성/즐거움) 섹션 — 정성 파이프라인 미적용 사유

raw data 38~53번 컬럼에는 이유(서술형) 컬럼이 존재하지만, 실제 보고서에는 이 섹션에서 평균·표준편차·레이더차트만 노출되고 인용문 분석이 등장하지 않는다(원본 검증 완료). 따라서 이 8개 문항은 **정량 처리 모듈로만 처리**하고, 6.2~6.3의 LLM 파이프라인을 적용하지 않는다. 원본 raw data의 이유 컬럼은 저장은 해두되(향후 확장 대비) 보고서 조립 시 사용하지 않는다.

### 6.8 핵심구매요소 상대중요도 산출식 (규칙 기반, LLM 불필요) — raw data 역산으로 확정

리바랩스 raw data(18~23번 컬럼, 1위~6위 순위 응답)로 원본 보고서의 상대중요도 수치를 역산 대조하여 산출식을 확정했다.

```
개인별 점수 = (후보 항목 수 + 1) − 2 × 해당 응답자가 매긴 순위
상대중요도(기능별) = 전체 응답자의 개인별 점수 평균
```

6개 항목 기준으로는 1위=+5점, 2위=+3점, 3위=+1점, 4위=-1점, 5위=-3점, 6위=-5점이 되어, 원본 보고서의 "중요 순위 응답을 0점 기준 ±5점 점수로 환산"이라는 설명과 정확히 일치한다. 후보 항목 수가 달라지면(예: 케어클은 8개) `(n+1)-2r` 공식의 n을 그대로 대입하면 된다.

| 기능 | raw data 역산 결과 | 원본 보고서 값 |
|---|---|---|
| 펫과의 산책 | 2.96 | 2.96 |
| 펫 성장 시스템 | 1.38 | 1.37 |
| 실시간 위치 기반 거점형 콘텐츠 | 0.26 | 0.26 |
| 펫 꾸미기 | 0.23 | 0.23 |
| 펫 레이싱 | -2.25 | -2.27 |
| 펫 교배 | -2.58 | -2.55 |

소수점 둘째 자리 이하의 미세한 차이는 반올림 오차로 추정되며, 산출식 자체는 확정된 것으로 간주한다. 이 계산은 규칙 기반으로 100% 재현 가능하므로 LLM 호출 없이 정량 처리 모듈(4장 아키텍처의 "순위/NPS 계산 모듈")에서 처리한다.

### 6.9 결과요약 생성 프롬프트 (Tier 1: 완전 자동생성)

```
[SYSTEM]
당신은 사용성테스트 결과보고서의 "사용성테스트 결과 요약" 섹션을
작성합니다. 아래 입력된 각 섹션의 통계 결과만을 근거로, 객관적
사실을 개조식으로 요약하세요.

# 규칙
- 해석이나 추천을 추가하지 마세요. 데이터에 있는 수치와 순위만
  사실 그대로 서술하세요. (해석은 이 섹션의 역할이 아닙니다.
  해석은 6.5절의 별도 제언 모듈에서 처리됩니다.)
- 4대가치, 핵심구매요소, 종합만족도·NPS 순서로 정리하세요.
- 개조식(명사형 종결, "~함", "~됨")으로 작성하세요.

[USER]
{전체 섹션 통계 요약 JSON}
```

---

## 7. Human-in-loop 설계

체크포인트는 별도 페이지가 아니라 **채팅 메시지 안에 렌더링되는 카드형 컴포넌트**(`PolarityReview.tsx`, `InsightEditor.tsx`, `ExtractionReview.tsx`)로 제공한다. 승인·수정은 카드 안의 버튼 클릭 또는 자연어 답장("이건 부정으로 바꿔줘") 둘 다로 가능해야 한다.

### 7.0 체크포인트 0 — 자유형식 요약 추출 검수 (`ExtractionReview` 컴포넌트, v1.3 신규)

**Tier 2를 자유 형식 문서(5.2절 방식B)로 제공했을 때만 발생한다.** Tier 1, Tier 2 정형 템플릿(방식A), Tier 3는 이 체크포인트를 거치지 않는다.

- `extractFreeformSummary` 도구가 원문 문서에서 추출한 통계·카테고리·인용문을 원문과 나란히 보여준다(좌: 원문 발췌, 우: 추출된 값).
- 사용자는 각 항목이 원문과 정확히 일치하는지, 섹션 매핑이 올바른지 확인하고 승인·수정한다.
- 신뢰도가 낮게 플래그된 항목(5.2절)을 우선 노출한다(체크포인트 A와 동일한 원칙, 6.4절 참조).
- 이 체크포인트를 통과하지 못하면 이후 단계(3장 ③ 이후)로 진행되지 않는다 — Tier 2·방식B는 AI의 해석이 개입하는 유일한 입력 경로이므로, fidelity 원칙(1.3절)을 지키기 위한 필수 관문으로 둔다.

### 7.1 체크포인트 A — 극성 판정 검수 (`PolarityReview` 컴포넌트)

- 6.4절 기준 "낮음" 신뢰도로 플래그된 clause만 노출한다(전수 노출 시 검수 피로 발생).
- 카드에는 clause 원문, LLM이 판정한 polarity, rationale을 같이 보여주고, 담당자가 원클릭으로 승인하거나 다른 극성으로 재배치할 수 있게 한다.
- 재배치 이력은 DB에 남겨, 추후 6.4절 신뢰도 규칙과 6.2절 few-shot 예시를 보강하는 데 사용한다.
- 승인 상태는 즉시 Vercel Postgres에 저장한다(4.3절 — 다음 채팅 턴에서 유실되지 않도록).

### 7.2 체크포인트 B — AI 생성 문장 검수 (`InsightEditor` 컴포넌트)

**대상 ①: 카테고리별 인사이트 문장 (6.3절 Stage2의 insight 필드)**
- 원래 실제 운영 프로세스에서는 관리자가 직접 쓰는 영역이므로, 라벨·인용문이 확정된 뒤에도 insight 필드는 바로 조립하지 않고 이 카드에서 노출한다.
- 문항×카테고리 단위로(리바랩스 기준 약 14개 문항 × 문항당 4~9개 카테고리 = 대략 100여 개) 빠르게 훑어보며 승인·수정할 수 있는 목록형 카드로 설계한다. **문항 단위로 접고 펼 수 있는 카드로 제공하는 것을 필수 요구사항으로 한다**(3.2절 UX 설계 원칙, v1.4에서 "고려한다"에서 격상 — 실사용 중 14문항이 통짜로 나열돼 검수 대상 파악이 어렵다는 문제가 실제로 확인됨). 6.3절 few-shot과 실제 원본 보고서 인사이트 문체를 기준으로 AI가 초안을 채워주므로, 담당자는 "전면 작성"이 아니라 "훑어보고 다듬기"에 가까운 작업이 되도록 한다.

**대상 ②: 제언 문단 (6.5절)**
- 6.5절에서 생성된 제언 문단을 카드 내 텍스트 편집 UI로 제시한다.
- 헤지 워딩이 실제로 지켜졌는지 규칙 기반으로 1차 검사(금지 표현 목록에 대한 문자열 매칭)하여, 위반 시 담당자에게 하이라이트로 표시한다.

둘 다 담당자 승인 없이는 최종 PDF에 포함되지 않는다(승인 상태는 DB 컬럼으로 관리, `assembleReport` 실행 전 확인).

### 7.3 필수 입력 — 종합 전략 제언

- AI가 생성하지 않는다. 담당자가 채팅으로 아래 내용을 자연어로 전달하면 그대로 반영한다.

| 항목 | 설명 |
|---|---|
| 고객사 요청사항 | 자유서술. (예: "마케팅을 활용한 매출 증대 방안 요청") |
| 우선 고려 지표 | 자연어 또는 선택지 제시(구매전환율 / 재구매율 / 신규유입 / 기타) |
| 제언 초안 | 담당자가 직접 작성. 단, 위 두 입력과 6.9절 결과요약을 컨텍스트로 제공하는 "초안 도와줘" 요청에는 AI가 초안을 만들되, 반드시 담당자가 전면 재작성 가능한 형태(채팅 내 편집 카드)로 제시 |

---

## 8. 출력 문서 조립 규칙

- **출력 포맷은 PDF, `@react-pdf/renderer`로 조립한다** (Vercel 서버리스 환경에 최적화된 선택 — Puppeteer 계열은 크로미움 바이너리 번들 문제로 배제. 상세 근거는 CLAUDE.md 참고).
- 3종 세트(그래프+표+서술) 자동 조립: Stage2 출력의 `categories` 배열을 순회하며, 카테고리별로 `[label]` 볼드체 + `quotes`를 따옴표·핵심어 볼드밑줄 처리(핵심어 자동 강조는 v1에서는 생략하고 원문 그대로 표기, 볼드밑줄 수동 강조는 v1.1 이후 검토)까지는 자동 조립한다. **`insight`는 체크포인트 B(7.2절)에서 담당자 승인을 받은 버전으로 교체된 뒤에만 화살표(→) 접두 + 볼드 처리하여 조립한다.** label·quotes가 확정되어도 insight 승인 전에는 문서가 발행되지 않는다.
- **그래프(만족도 분포도, 레이더차트 등)는 react-pdf가 직접 그리지 못하므로, 별도 차트 라이브러리로 PNG 이미지를 먼저 생성한 뒤 `<Image>` 컴포넌트로 삽입한다.**
- 표는 react-pdf의 flexbox 레이아웃으로 구성한다(CSS Grid 미지원 — 원본 보고서의 복잡한 표 레이아웃 재현에 제약이 있을 수 있음, 11장 리스크 참조).
- 한글 폰트(Noto Sans KR 등)는 명시적으로 등록해야 한다(기본 폰트로는 한글이 깨짐).
- 정량 통계는 규칙 기반 계산 결과를 표+막대그래프로 렌더링.
- 섹션 순서는 표준스키마_SW앱형.pdf의 목차(Ⅰ~Ⅸ)를 그대로 따른다.

---

## 9. 품질 검증

### 9.1 검증 우선순위 — 외부 근사도 우선

내부 정합성(raw data 그대로 계산했을 때 계산이 정확한가)과 외부 근사도(실제 발행 보고서와 얼마나 비슷한가) 중, **외부 근사도를 우선순위로 검증한다.**

리바랩스는 raw data(100건)와 실제 보고서가 완전히 일치하는 레퍼런스이므로, 두 기준이 사실상 동일하게 적용된다. 따라서 v1의 골든 테스트셋은 **리바랩스로 고정**한다.

> 참고: 케어클(실물제품형, v2 대상) raw data는 27건이나 실제 보고서는 25건 기준으로 산출되어 있어(원인 불명의 담당자 필터링으로 추정) 외부 근사도가 100%에 도달할 수 없는 구조다. v2 검증 기준 설계 시 이 점을 반드시 별도로 명시해야 하며, 이 차이를 "시스템 결함"으로 오판하지 않도록 주의한다.

### 9.2 섹션 성격별 측정 방법

| 섹션 성격 | 측정 방법 |
|---|---|
| 정량 섹션(평균/표준편차/%) | 실제 보고서 수치와의 오차(delta). 리바랩스 기준 오차 0 목표 |
| 카테고리 분류 | 실제 보고서 `[카테고리명]`과 생성 결과 카테고리의 주제 일치율(커버리지 %) |
| 극성 판정 | raw data 특정 문장이 실제 보고서에서 배치된 극성과 생성 결과 극성의 일치율 (원문 대조 가능한 표본 기준) |
| 인사이트 문장 | 사람 정성 리뷰(자동측정 불가) |

### 9.3 회귀 테스트 방식

리바랩스 raw data를 v1 파이프라인에 통과시켜 생성된 결과를 원본 보고서와 항목별로 대조하는 스크립트를 CI에 포함한다. 프롬프트나 로직을 변경할 때마다 이 회귀 테스트를 재실행하여 fidelity 지표(1.3절)가 하락하지 않았는지 확인한다.

### 9.4 정성 리뷰 체크리스트 (인사이트 문장용)

- [ ] 원본 보고서의 인사이트 문장과 톤이 유사한가(관찰형 vs 단정형)
- [ ] 화살표+볼드 서식이 정확히 적용되었는가
- [ ] 카테고리명이 지나치게 포괄적이지 않은가(6.3절 세분화 지침 위반 여부)

---

## 10. 비기능 요구사항

- **배포 플랫폼**: Vercel, Fluid Compute 활성화(기본 함수 실행시간 300초, Pro 요금제에서 최대 800초까지 확장 가능).
- **데이터 프라이버시**: raw data는 응답자 개인정보(나이·성별 등)를 포함하므로, LLM API 호출 시 개인식별정보 최소화 원칙 적용(respondent_id는 익명 일련번호만 사용, 실명·연락처 등은 애초에 컬럼에 없음을 5장에서 확인함).
- **처리 시간 (필수 요구사항으로 격상)**: 문항 14개 × 2단계 = 28회 호출을 **반드시 병렬 처리**한다(4.2절). 순차 처리 시 10~20분이 소요되어 Vercel 함수 실행시간을 초과하므로, 문항 간 동시 처리(`p-limit`으로 동시 5~10개 제한)가 v1의 필수 구현 조건이다. Phase 4·9(12장)에서 실측 시간을 반드시 검증한다.
- **상태 관리**: 서버리스 함수는 요청마다 독립 실행되므로(stateless), raw data 파싱 결과·파이프라인 중간 산출물·체크포인트 승인 상태는 Vercel Postgres에 영속화한다(4.3절).
- **재현성**: 동일 raw data 재업로드 시 완전히 동일한 결과가 나오지 않을 수 있음(LLM 특성). temperature=0 설정 및 회귀테스트 기준 허용 오차 범위 사전 정의 필요.
- **확장성**: 응답자 수(n)가 커질 경우(예: 500명 이상) 문항당 1콜로 전체 응답을 넣는 현재 설계가 컨텍스트/정확도 한계에 부딪힐 수 있음 → 청크 분할 로직을 v1.1 이후 검토(11장 참조).
- **API 레이트리밋**: 문항 간 병렬 호출이 Claude API의 분당 요청 제한에 걸리지 않도록 동시성 상한을 실제 API 티어 기준으로 설정해야 한다(11장 참조).
- **파일 업로드 아키텍처(v1.3 신규)**: 파일당 최대 **500MB**까지 지원한다(Vercel Blob 캐싱 기준점인 512MB 바로 아래로 설정 — 이 이하는 CDN 캐싱되어 조회가 빠르고, 이상은 매 조회마다 추가 전송비용이 붙음). Vercel Functions는 요청 바디 크기가 4.5MB로 고정 제한되어 있어, 이 한도를 우회하려면 **파일이 서버를 거치지 않고 브라우저에서 Vercel Blob으로 직접 전송되는 클라이언트 업로드 방식**(`@vercel/blob/client`의 `upload()`, 토큰 기반 인증)을 반드시 사용해야 한다. 서버 업로드 경로(파일이 Route Handler를 거치는 방식)로 구현하면 500MB 목표를 달성할 수 없다.
- **지원 파일 형식**: raw data(xlsx), 구조화된 요약본 템플릿(xlsx), 자유 형식 요약본(docx/pdf/txt), 제품 정보 이미지(jpg/png/webp).

---

## 11. 리스크 및 열린 질문

1. **비표준 컬럼 포맷은 어떻게 할 것인가** — v1은 지원하지 않기로 확정했으나, 실무에서 WALLA 외 다른 툴로 수집되는 경우가 생기면 재논의 필요
2. **극성 판정에서 점수(정량 응답)의 실제 역할 비중** — 6.2절 프롬프트는 "텍스트가 애매할 때만 점수를 보조 신호로 참고"하도록 설계했다. 이는 "모드전환 7점+두 개의 다른 이유" 사례(점수는 동일한데 텍스트만으로 분류가 갈림)와는 부합하지만, 원 프로세스 설명("정량 수치까지 종합하여 판단")을 문자 그대로 반영한 것인지는 확실하지 않다. 점수가 더 적극적으로 판정에 개입하는 사례가 실무에 별도로 있는지 확인 전까지는 가설로 취급한다.
3. **극성 판정 신뢰도 임계치(6.4절)** — 현재는 "감정어 포함 여부"라는 단순 휴리스틱. 체크포인트 A 운영 로그가 쌓이면 더 정교한 기준으로 교체 필요
4. **대응답자 규모(n>200~300)에서의 청크 분할 전략** — 현재 설계는 리바랩스(n=100) 기준 검증됨. 더 큰 데이터셋에서는 문항당 1콜 방식이 정확도를 담보하지 못할 수 있음
5. **케어클(실물제품형) 필터링 미스터리** — 27건→25건이 된 기준을 알 수 없는 채로 v2를 설계해도 되는지, 아니면 원 담당자에게 직접 확인이 필요한지
6. **재현성 허용 오차** — LLM 기반 정성 처리는 매번 100% 동일한 카테고리 분할이 나오지 않을 수 있음. 회귀테스트에서 어느 정도의 변동을 "정상"으로 볼지 기준 미정
7. ~~핵심구매요소 상대중요도 산출식~~ — **해결됨.** `개인별 점수=(후보 항목 수+1)-2×순위`의 응답자 평균으로 확정(6.8절, raw data 역산 검증 완료)
8. **카테고리별 인사이트 작성 주체(신규)** — 6.3절 수정으로 Tier2(AI초안+필수편집)로 전환했으나, 실제로 담당자가 "훑어보고 다듬는" 수준인지 "사실상 새로 쓰는" 수준인지에 따라 체크포인트 B의 UI 설계(7.2절)가 크게 달라진다. 실제 운영 데이터로 확인 필요
9. **Claude API 레이트리밋 실측(v1.1 신규)** — `p-limit` 동시 호출 수(5~10개)는 잠정치이며, 실제 계정의 API 티어(분당 요청 수 제한)를 확인해 Phase 4·9에서 조정해야 한다. 너무 낮게 잡으면 처리시간이 늘어 Vercel 타임아웃에 다시 가까워지고, 너무 높게 잡으면 API 에러(429)가 발생한다.
10. **react-pdf의 레이아웃 제약(v1.1 신규)** — flexbox만 지원하고 CSS Grid를 지원하지 않아, 원본 보고서의 복잡한 표·좌우 분할 레이아웃을 얼마나 재현할 수 있는지 Phase 7에서 실제로 확인해야 한다. 재현이 어려운 요소가 발견되면 대안(레이아웃 단순화 또는 Puppeteer 재검토)을 다시 논의한다.
11. **Vercel 함수 실행시간 여유(v1.1 신규)** — 병렬 처리를 적용해도 실제 처리시간이 300초에 얼마나 여유 있게 들어오는지는 Phase 4·9의 실측 전까지 확정되지 않는다. 여유가 부족하면 Pro 요금제 업그레이드(800초) 또는 Vercel Workflows 검토가 필요하다.
12. **Batch API의 진행 중 상태 조회 가능 여부(v1.1 신규)** — Anthropic 공식 문서 사이에서도 서술이 엇갈린다. Batch 조회 API의 응답 예시는 `processing_status: "in_progress"` 상태에서도 `request_counts.succeeded`가 채워져 있는 것처럼 보이지만, 배치 목록 API 문서는 "succeeded 카운트는 전체 배치가 끝날 때까지 0으로 유지된다"고 명시한다. 이 항목은 13장의 실험 계획으로 실측 검증한다.
13. **Tier 2 자유 형식 문서 추출의 정확도(v1.3 신규)** — `extractFreeformSummary`는 AI가 문서 레이아웃을 해석해 값을 찾아내는 작업이라, Tier 1(결정론적 컬럼 파싱)이나 Tier 2 정형 템플릿보다 오류 가능성이 구조적으로 높다. 체크포인트 0(7.0절)으로 보완하지만, 실제 다양한 문서 양식(표 중심/서술형 등)에 대한 추출 정확도는 골든 테스트셋만으로 검증하기 어렵다 — 실제 사용자가 제공하는 문서 샘플을 축적하며 지속 검증이 필요하다.
14. **Tier 2 정형 템플릿의 정확한 컬럼 스펙(v1.3 신규)** — 5.2절 방식A의 시트·컬럼 구성은 초안이며, Phase 1에서 실제 사용자 시나리오를 반영해 확정해야 한다.

이 중 9번(레이트리밋), 12번(Batch 진행률)을 포함한 토큰 사용 전략 관련 항목은 13장에서 체계적인 실험 계획으로 다룬다.

---

## 12. 개발 로드맵

| Phase | 내용 | 산출물 |
|---|---|---|
| Phase 1 | 채팅 셸 + 파일 업로드 (Next.js 프로젝트, AI SDK 채팅 엔드포인트, Vercel Blob 연동) | raw data 업로드 후 "파일 받았습니다" 수준의 응답까지 확인 |
| Phase 2 | 입력 검증 + 정량 처리 모듈 (5장 스키마 파싱 + 통계 계산 + 6.8절 상대중요도 공식·NPS 공식) | 정량 섹션 100% 자동 생성, 리바랩스 대비 오차 0 검증, 채팅으로 결과 스트리밍 |
| Phase 3 | Stage1 모듈 (`generateObject`+Zod로 문장분리+극성판정) | few-shot 예시 재현 확인 |
| Phase 4 | Stage2 모듈(카테고리+인용) + 신뢰도 스코어링 + **병렬 처리 구현**(4.2절) | 14개 문항 병렬 처리 실측 시간 보고 (Vercel 300초 대비 여유 확인) |
| Phase 5 | 체크포인트 A/B 인라인 컴포넌트 (채팅 내 카드 UI, DB 상태 저장) | 검수·편집 가능한 채팅 인라인 UI |
| Phase 6 | 제언 생성 모듈(헤지워딩) + 결과요약 모듈 + 종합전략제언 입력 UI | 전체 텍스트 섹션 생성 |
| Phase 7 | 출력 문서 조립 (8장, react-pdf + 차트 이미지 변환) | PDF 산출물, 원본 보고서와 레이아웃 대조 |
| Phase 8 | 회귀테스트·fidelity 측정 자동화 (9장) | CI 파이프라인 |
| Phase 9 | 동시성·레이트리밋 튜닝 + **13장 실험 계획 실행** | 안정적인 처리시간·에러율 확보, 토큰 사용 전략 확정 |

---

## 13. 토큰 사용 전략 검증 실험 계획 (v1.1 신규)

### 13.1 목적

"높은 퀄리티를 유지하면서 빠르게 보고서를 생성한다"는 목표를, 감이나 임의 판단이 아니라 **골든 테스트셋 기반 실측**으로 뒷받침한다. 여러 설계 선택지(모델, API 방식, 동시성 수준, 호출 구조) 중 무엇이 실제로 유리한지 Phase 9(또는 그 전 스파이크 단계)에서 검증한다.

### 13.2 공통 실험 방법론

- 모든 실험은 **리바랩스 골든 테스트셋**(raw data 100건)을 입력으로 사용한다.
- 비교 기준(baseline)은 현재 확정 설계: Sonnet 계열, Stage1/Stage2 분리, 표준 API, `p-limit` 동시 5~10개.
- 측정 지표는 1.3절 fidelity 기준(정량 오차, 카테고리 커버리지, 극성 판정 일치율, 인용문 정확성)과 9.2절 측정 방법을 그대로 재사용하고, 아래 2가지를 추가한다.
  - **처리 시간**: 문항 14개 처리 완료까지 걸린 wall-clock 시간
  - **비용**: 보고서 1건 생성 기준 실제 청구 토큰 비용
- **한 번에 한 변수만 바꾼다.** baseline 대비 단일 변수 변경 후 비교하며, 여러 변수를 동시에 바꾸지 않는다.
- 실험 결과와 채택 여부는 `docs/architecture_decision_log.md`에 실험일자·방법·수치·결정을 기록한다. PRD 본문에는 최종 채택된 결정만 반영한다.

### 13.3 실험 항목

| # | 실험 | 방법 | 측정 지표 | 성공 기준 |
|---|---|---|---|---|
| 1 | Batch API 진행 중 상태 조회 가능 여부 (11장 12번) | 실제 소규모 배치(14개 요청) 생성 후 짧은 간격으로 폴링, `request_counts`가 배치 종료 전에 갱신되는지 로그로 기록. 완료까지 걸린 시간도 3회 이상 반복 측정 | 종료 전 카운트 갱신 여부(불리언), 완료 소요시간 분포 | 카운트가 실시간 갱신되고 평균 완료시간이 5분 이내로 안정적이면 하이브리드 방식(집계는 Batch 폴링, 상세 결과는 완료 후 조회) 재검토. 둘 중 하나라도 불충족 시 표준 API 유지 |
| 2 | Claude API 레이트리밋 및 최적 동시성 수준 (11장 9번) | `p-limit` 값을 3/5/10/14로 바꿔가며 14개 문항 파이프라인 각 3회 이상 반복 실행 | 총 처리시간, 429 에러 발생률 | 에러율 0%를 유지하는 가장 높은 동시성 값을 채택 |
| 3 | Stage1 모델 대체 (Sonnet → Haiku) | Stage1만 Haiku로 교체해 골든 테스트셋 전체 재실행, Sonnet 기준 결과와 문항별·응답자별 극성 판정을 대조 | 극성 판정 일치율(Sonnet 대비), 처리시간, 비용 | 부정/중립 경계(6.4절) 일치율 하락폭이 5%p 이내면 채택 검토, 초과 시 Sonnet 유지 |
| 4 | Stage1+Stage2 통합 단일 호출 | 두 단계를 하나의 프롬프트/호출로 합친 변형 파이프라인을 프로토타입으로 구현, 골든셋으로 실행 | 카테고리 커버리지, 인용문 verbatim 정확성, 처리시간, 비용, 호출 수 | 정확성 지표가 2단계 분리 방식 대비 유의미하게 떨어지지 않으면 채택(하락 허용폭은 실험 착수 시 재논의) |
| 5 | 프롬프트 캐싱 적용 효과 | 캐싱 켠 상태·끈 상태로 동일 파이프라인 각 3회 이상 실행 | 입력 토큰 비용 절감폭, 출력 결과 동일성(캐싱이 결과 내용에 영향 주지 않는지 확인) | 절감이 확인되고 결과가 동일하면 기본 적용 |

### 13.4 실행 순서

비용·시간이 적게 드는 것부터, 그리고 이후 실험들의 기준 타이밍에 영향을 주는 인프라 실험을 먼저 진행한다.

1. **실험 2(동시성/레이트리밋)** — 가장 기초적인 인프라 검증이며, 이후 모든 실험의 처리시간 측정이 이 값에 의존한다.
2. **실험 1(Batch 진행률)** — 비용이 거의 들지 않고 빠르게 결론 낼 수 있다.
3. **실험 5(프롬프트 캐싱)** — 구현 부담이 적다.
4. **실험 3(Stage1 모델 대체)** — 정확도 검증에 시간이 걸린다.
5. **실험 4(Stage1+2 통합)** — 구조 변경 폭이 가장 크므로 마지막에 진행한다.

---

## 부록 A. 제품유형별 섹션 구성 비교

### A.1 실물제품형 전용 섹션 구성 (케어클 기준, v2 대상)

Ⅰ 개요 → Ⅱ 인적사항 및 특성조사 → Ⅲ 기능별 고객경험평가 → **Ⅳ 고객여정 기반 경험평가** → Ⅴ 핵심구매요소 → Ⅵ 4대가치 만족도 → Ⅶ NPS 종합만족도 및 개선아이디어 → Ⅷ 종합결과 및 제언(하위: 구매전환율/재구매율 개선전략)

### A.2 SW/App형 전용 섹션 구성 (리바랩스 기준, **v1 빌드 대상**)

Ⅰ 개요 → Ⅱ 인적사항 및 특성조사 → Ⅲ 기능별 고객경험평가 → Ⅳ 핵심구매요소 → Ⅴ 4대가치 만족도 → **Ⅵ 사용자 경험 품질평가** → **Ⅶ 교차분석** → Ⅷ NPS 종합만족도 및 개선아이디어 → Ⅸ 종합결과 및 제언(하위: 개발우선순위제언)

### A.3 차별점 비교표

| 항목 | 실물제품형 | SW/App형 |
|---|---|---|
| 고객여정 기반 경험평가 | 포함(필수) | 미포함 |
| 사용자경험 품질평가(실용성/즐거움) | 미포함 | 포함 |
| 교차분석(연령·성별) | 미포함 | 포함 |
| 개선전략제언 하위항목 | 구매전환율/재구매율 개선전략 | 개발우선순위제언 |
| raw data ↔ 보고서 일치도 | 27건→25건(불일치, 원인불명) | 100건→100건(완전 일치) |
| 정성 파이프라인 검증 상태 | 검증 완료(케어클로 최초 도출) | **검증 완료(리바랩스로 재검증, 본 PRD 기준)** |

### A.4 통합본(하이브리드) 섹션 구성 — v3 목표 설계 (v1·v2 빌드 대상 아님, v1.2 갱신)

두 제품유형을 하나의 시스템으로 통합할 때, 제품유형 플래그(`product_type`)에 따라 섹션을 조건부로 삽입하는 방식을 제안한다.

```
Ⅰ. 개요
Ⅱ. 인적사항 및 특성조사
Ⅲ. 기능별 고객경험평가
Ⅳ. 고객여정 기반 경험평가         [조건: product_type == PHYSICAL_PRODUCT]
Ⅴ. 핵심구매요소
Ⅵ. 4대가치 만족도
Ⅶ. 사용자 경험 품질평가           [조건: product_type == SW_APP]
Ⅷ. 교차분석                      [조건: product_type == SW_APP]
Ⅸ. NPS 종합만족도 및 개선아이디어
Ⅹ. 종합결과 및 제언
    └ 개선전략제언 하위항목:
        product_type == PHYSICAL_PRODUCT → 구매전환율/재구매율 개선전략
        product_type == SW_APP → 개발우선순위제언
```

이 통합 설계는 v1·v2에서는 채택하지 않지만, **v3("기본" 사용성테스트 보고서)의 확정된 목표 설계다**(2.2절 제품 로드맵 참조). v1과 v2가 각각 독립적으로 fidelity 검증(1.3절 기준)을 마친 뒤, 이 조건부 섹션 구성 로직을 실제 구현으로 옮긴다. 제품유형 판별 방식(사용자 명시 vs raw data 자동 추론)과 입력 검증 모듈의 분기 방식은 v3 착수 시점에 별도로 확정한다(2.2절 참조).

---

## 부록 B. 예상 토큰 비용 산정

### B.1 산정 기준

리바랩스 raw data(100명) 기준, 정성 처리 대상 14개 문항의 실제 텍스트 분량을 측정하여 산정했다. 한국어 텍스트는 보수적으로 1.6자/토큰으로 환산했다(Claude 공식 안내는 영어 기준 "1토큰≈4자"이며, 한국어는 토큰 효율이 낮으므로 별도 환산치 적용. **실제 빌드 시 Claude API의 토큰 카운트 엔드포인트로 재검증 필요**).

### B.2 문항별 실측 텍스트 분량

| 질문 | 원문 글자수 | 응답당 평균 | 추정 원문 토큰 |
|---|---|---|---|
| 펫과의 산책 | 12,874자 | 128.7자 | 8,046 |
| 펫 성장 시스템 | 10,479자 | 104.8자 | 6,549 |
| 펫 꾸미기 | 8,695자 | 87.0자 | 5,434 |
| 실시간 거점형 | 9,235자 | 92.3자 | 5,772 |
| 펫 교배 | 8,744자 | 87.4자 | 5,465 |
| 펫 레이싱 | 11,019자 | 110.2자 | 6,887 |
| 유사서비스 만족도 이유 | 8,969자 | 89.7자 | 5,606 |
| 기능적가치 | 8,170자 | 81.7자 | 5,106 |
| 심미적가치 | 7,033자 | 70.3자 | 4,396 |
| 경제적가치 | 7,066자 | 70.7자 | 4,416 |
| 사회공공적가치 | 7,592자 | 75.9자 | 4,745 |
| 전반적만족도 | 7,167자 | 71.7자 | 4,479 |
| NPS | 5,148자 | 51.5자 | 3,218 |
| 개선아이디어 | 16,826자 | 168.3자 | 10,516 |
| **합계** | **129,017자** | — | **80,636** |

### B.3 보고서 1건 생성 기준 총 토큰·호출 수

Stage1 고정 프롬프트(지침+few-shot) 약 1,400토큰, Stage2 고정 프롬프트 약 1,200토큰을 문항마다 반복 사용한다고 가정(캐싱 미적용 기준)하고, Stage1 출력은 원문 대비 1.35배(절 분리+판정근거 추가로 구조화 오버헤드 발생), Stage2 출력은 Stage1 출력의 35% 수준(카테고리+대표인용 발췌+인사이트, 원문 일부만 재인용)으로 추정했다.

| 항목 | 값 |
|---|---|
| Stage1+2 총 입력 토큰 | 225,894 |
| Stage1+2 총 출력 토큰 | 146,958 |
| 제언/요약 모듈(4콜) 입력·출력 | 7,800 / 2,700 |
| **총 입력 토큰** | **233,694** |
| **총 출력 토큰** | **149,658** |
| **총 API 호출 수** | **32회** (정성 파이프라인 28 + 제언/요약 4) |

### B.4 모델별 예상 비용 (보고서 1건 생성 기준)

Anthropic 공식 가격(2026.7 기준, platform.claude.com/docs 확인)을 적용했다.

| 모델 | 표준 API | Batch API(50% 할인) |
|---|---|---|
| **Claude Sonnet**(표준가 $3/$15 — Sonnet 4.6 및 2026.9.1 이후 Sonnet 5) | **$2.95** | **$1.47** |
| Claude Sonnet 5 (도입가, ~2026.8.31, $2/$10) | $1.96 | $0.98 |
| Claude Haiku 4.5 ($1/$5) | $0.98 | $0.49 |

**권장안 (v1.1 수정)**: 정성 판정 정확도가 fidelity 성공기준(1.3절)에 직결되므로 v1은 **Sonnet 계열**을 기본값으로 권장한다.

~~보고서 생성은 실시간 응답이 필요한 워크플로우가 아니므로 Batch API 적용을 권장한다~~ — **채팅 기반 UI로 확정하면서 이 판단을 철회한다.** Batch API는 처리에 최대 24시간까지 걸릴 수 있는 비동기 방식이라, 3장의 대화형 흐름(사용자가 채팅 안에서 진행 상황을 실시간으로 보며 체크포인트를 승인하는 구조)과 맞지 않는다. v1은 **표준 API + 병렬 처리(4.2절)**로 응답성을 확보하는 쪽을 기본값으로 하며, 이 경우 보고서 1건당 약 **$2.95**(Sonnet 표준가 기준)가 예상된다. Batch는 향후 "야간 일괄 처리" 같은 별도 사용 시나리오가 생기면 그때 재검토한다.

> 참고 — Sonnet 5는 2026.8.31까지 도입가($2/$10)가 적용되나, Sonnet 5는 신규 토크나이저를 사용해 동일 텍스트에 대해 기존 대비 약 30% 더 많은 토큰을 소비한다. 도입가 기간에는 이 토큰 증가를 상쇄하고도 Sonnet 4.6보다 저렴하지만, 2026.9.1 표준가($3/$15) 적용 후에는 토큰 증가분 때문에 실질 비용이 Sonnet 4.6보다 높아질 수 있다. 빌드 시점의 최신 가격을 다시 확인할 것을 권장한다.

### B.5 추가 절감 여지 — 프롬프트 캐싱

Stage1·Stage2의 고정 프롬프트(지침+few-shot 예시) 부분은 문항마다 반복 사용되며 총 36,400토큰(전체 입력의 15.6%)에 달한다. 이 부분을 프롬프트 캐싱(cache read는 기본 입력가의 10%)으로 처리하면 추가로 입력 비용을 절감할 수 있다. Batch를 쓰지 않기로 한 v1.1 기준에서는 **프롬프트 캐싱이 사실상 유일한 비용 절감 레버**이므로, Phase 3~4(6장 Stage1/2 구현) 시 캐싱 적용을 기본으로 설계할 것을 권장한다.

### B.6 월간 운영 비용 추정 (참고, v1.1 수정)

월 20건의 보고서를 생성한다고 가정하면(표준 API 기준) **월 약 $59 내외**로, 프롬프트 캐싱 적용 시 이보다 낮아질 것으로 예상된다(Batch를 쓰지 않으므로 B.4의 $2.95 × 20건 기준이며, 이전 버전의 Batch 적용 추정치 $30보다 높아진 점에 유의). 이는 담당자 1인의 수작업 시간(보고서 1건당 통상 며칠 소요로 추정)과 비교하면 여전히 상당히 낮은 비용이나, 정확한 ROI 비교를 위해서는 현재 수작업 소요 시간에 대한 별도 확인이 필요하다.

---

*이 PRD는 리바랩스 raw data(100건, 59컬럼) 실측 검증을 기반으로 작성되었다. 상대중요도 산출식(6.8절)은 raw data 역산으로 확정 완료했다. v1.1에서 UI·배포 아키텍처(채팅 기반, Vercel/Next.js 단일 스택)를 확정했으며, v1.2에서 제품 로드맵(v1→v2→v3)을 공식화했다. 6장의 프롬프트 본문은 이 변경들의 영향을 받지 않는다. 11장에 남은 나머지 열린 질문은 Phase 1~2 착수 전 순차 확인이 필요하다.*
