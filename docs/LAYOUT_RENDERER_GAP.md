# 레이아웃 렌더러 실측 갭 목록 (Phase 12b)

PRD 2.2.2절 5단계(레이아웃 렌더)에 들어가기 전에, **레이아웃 카탈로그 40종이 지금 실제로
어떻게 그려지는지**를 렌더로 확인한 결과다. "기존 블록을 그대로 쓰면 된다"는 앞선 판단이
어디까지 맞고 어디서 틀리는지를 코드가 아니라 그림으로 확정하는 것이 목적이다.

**측정 방법**: 웹 작업공간(`/viewer` 데모, 리바랩스 정량)을 헤드리스 Chrome으로 PDF 출력(41쪽)
→ 원본 발행 보고서(`data/[알파브라더스] 리바랩스_..._0904_상연.pdf`, 56쪽)의 같은 장과 대조.
레이아웃 코드·조건은 `docs/STAGE_MAPPING.xlsx`, 시각 기준은
`docs/사용성테스트 결과 보고서 레이아웃 v.0.5.pdf`.

**측정일**: 2026-08-25. 데모에는 정성 분석 결과가 없어 정성 블록(L11 계열)은 "대기" 상태로
렌더된다 — 구조는 확인 가능하나 내용 대조는 정성 분석이 있는 보고서로 다시 봐야 한다.

---

## 0. 결론

> **2026-08-25 진행**: 아래 표는 최초 측정 결과이고, 그 뒤 수정한 항목은 각 줄에 정정·완료로 표시했다.
> 측정 과정에서 **내 최초 판정 세 개가 틀렸다**(Ⅲ장 골격 없음 / 장 내부 순서 반대 / 사분면이 산점도여야 함).
> 셋 다 원본 PDF와 코드를 다시 열어 확인해 바로잡았다 — 렌더 한 장면만 보고 구조를 단정하면 안 된다는 교훈.

| 판정 | 최초 측정 | 지금 | 뜻 |
|---|---|---|---|
| ✅ 전용 컴포넌트 | 12 | **21** | 데이터가 바뀌어도 형태가 유지된다. 그대로 재사용 |
| ❗ 있지만 원본과 다름 | 8 | 1 | 렌더는 되는데 원본과 형태·축·순서가 어긋난다 |
| ⚠️ HTML 문자열 | 9 | 8 | 리바랩스 전용 HTML이 빌더 안에 박혀 있다 |
| ❌ 없음 | 11 | 2 | 렌더러 자체가 없다(L14 경쟁재 비교표 · L20 경험 수준 정의표) |

**"새 블록을 만들 필요가 없다"는 앞선 판단은 절반만 맞았다.** 블록 `kind`(데이터 계약)는
대부분 재사용 가능했지만 렌더러는 아니었다.

**❌ 11종을 하나씩 코드·원본과 대조한 결과(2026-08-25)**: 3종은 **이미 구현돼 있었고**
(L12·L25·L26), 1종은 기존 블록이 그대로 맞았으며(L19), 3종은 새로 만들었다(L13·L13a·L27).
실제로 남은 것은 **2종뿐**이다(L14·L20). 처음 세었던 11은 문서상 대조의 결과였고, 코드를
열어보니 절반 이상이 이미 있었다 — 이 문서의 판정을 근거로 새로 만들기 전에 반드시 해당
빌더 코드부터 열어볼 것.

---

## 1. 코드별 판정

### 차트

| 코드 | 이름 | 현재 구현 | 판정 | 실측 내용 |
|---|---|---|---|---|
| L1 | 세로 막대 | `EditableBarChart` | ✅ | 값 라벨·그리드·배너 모두 있음 |
| L1a | 세로 막대+평균선 | `EditableBarChart` | ✅ | ~~웹 0~9점 / PDF 4~8점~~ **해결(2026-08-25)**: 축 규칙을 `lib/report/chartAxis.ts` 한 곳으로 옮겨 웹·PDF·DOCX가 공유한다. 눈금도 정수(4/5/6/7/8)로 맞췄다 |
| L1b | 만족도 분포도 | `satisfactionHistogramSvg`(HTML 문자열) | ⚠️ | SVG 생성기는 있으나 rich-static으로 주입 |
| L2 | 가로 누적(교차표) | `EditableStackedBarChart` | ✅ | |
| L3 | 그룹 막대 | `EditableGroupedBarChart` | ✅ | 37줄. 원본 대조는 미실시 |
| L4 | 레이더 | `EditableRadarChart` | ✅ | |
| L4a | 레이더 오버레이 | `EditableRadarChart`(다중 시리즈) | ✅ | |
| L5 | 순위 구성 누적막대 | `EditableRankCompositionChart` | ✅ | |
| L6 | 사분면 | `EditableQuadrantChart` | ✅ | ~~원본은 산점도~~ **정정(2026-08-25)**: 원본(28쪽)도 격자 칸에 항목명을 넣는 방식이 맞다. 경계(-2/+2, 6/8)·9칸 배치도 FGI 원본과 일치했다. 실제 문제는 ① 존 색 4개가 근사값이었던 것 ② 긴 라벨이 칸을 넘쳐 이웃과 겹친 것 ③ y축 제목이 눕혀져 있던 것 — 셋 다 수정 완료 |
| L6a | 영역별 참고 지표 | `PriorityReferenceDiagram` | ✅ | |
| L7 | NPS 척도 | `EditableNpsChart`(원본 PNG) | ✅ | 원본 이미지를 그대로 씀 |
| L8 | 극성 3분할 | `EditablePolarityChart` + `donutSvg` | ❗ | 원본은 **반원 도넛**. 두 표현이 공존해 어느 것이 정본인지 불명확 |
| L12 | 워드클라우드 | 이미지 첨부 슬롯(`ReportImageUploadSlots`) | ⚠️ | ~~없음~~ **정정(2026-08-25)**: 자리는 원본과 같은 2열 오른쪽 칸에 이미 있고, 담당자가 PNG를 붙이는 방식이다. 자동 생성만 없다(PRD 2.3 제외 항목) |
| L13 | 시점 추이 꺾은선 | `EditableJourneyLineChart` | ✅ | **구현(2026-08-25)**: 케어클 원본 34쪽 실측(선 #667eea, 배경 #eff1fd, 0~10 정수축, 점마다 값 라벨). 시점 순서는 정렬하지 않는다 |
| L13a | 만족도 변화 워터폴 | `EditableWaterfallChart` | ✅ | **구현(2026-08-25)**: 구간 증감을 시점 목록에서 계산한다(꺾은선과 어긋날 수 없게). 상승 #71bf73 / 최대 하락 #f6695f / 그 외 하락 #ffad33 |
| L19 | 추천 대상 분포 | `chartBlock`(세로 막대) | ✅ | **정정(2026-08-25)**: 카테고리별 세로 막대(명)라 기존 블록이 그대로 맞다. 새 렌더러 불필요 — 선행 조건은 자유서술 카테고리화(파이프라인 쪽 일) |

### 표

| 코드 | 이름 | 현재 구현 | 판정 | 실측 내용 |
|---|---|---|---|---|
| L9 | 일반 표 | `EditableTable` | ✅ | **해결(2026-08-25)**: 제목을 표 첫 행으로 넣고(원본은 한 테두리로 이어짐), 계열별 팔레트·11pt·행높이 23pt·라벨열 배경·소수 2자리를 원본 실측값으로 맞췄다 |
| L9a | 전치 순위표 | `tableBlock` | ⚠️ | PDF는 행 기반 `TransposedRankTable`로 고쳤는데 웹은 일반 표. 긴 항목명 줄바꿈 시 어긋날 위험(PDF에서 겪은 버그와 같은 구조) |
| L9b | 설문 항목 표 | HTML 문자열 | ⚠️ | 병합 셀 표라 rich-static으로만 존재 |
| L8t | 극성 건수 표 | `polarityTableHtml` | ⚠️ | |
| L14 | 경쟁재 비교표 | — | ❌ | **원자료는 있다**(이젠오토 26~29번, 정리습관 34~38번에 서비스명·장점·단점이 별도 컬럼으로 있음 — 2026-08-26 raw data 직접 확인). 렌더러는 셀 안 불릿 + 행 병합이라 설문 항목 표와 같은 rich-static 패턴. **다만 원본(이젠오토 8쪽)은 45명의 자유서술을 서비스별로 묶고 장·단점을 짧은 불릿으로 요약한 결과다** — 표만 그리면 45행짜리 다른 표가 된다 |
| L18 | 전략 과제 도출표 | `workspaceConclusion`(rowGroup/HTML 혼재) | ⚠️ | 원본 대조 미실시 |
| L20 | 경험 수준 정의표 | — | ❌ | 이젠오토 원본 7쪽 재확인: 좌측 **세로 병합 라벨**("경험 수준") + 단계명 + 설명문 3열. 설명문은 raw data 응답값이 아니라 **설문 설계 정보**라 입력 경로부터 정해야 한다(아래 4절) |
| L21 | 기능별 고객 제언 종합 | `workspaceConclusion` | ⚠️ | 원본 대조 미실시 |
| L22 | NPS 요약표 | HTML 문자열 | ⚠️ | |
| L23 | 결과 요약 항목별 표 | `rowGroupBlock` | ✅ | 표 전체를 한 블록으로 그리도록 이미 수정됨 |
| L25 | 긍정·부정 주요 의견 표 | `valueOpinionColumnHtml` | ✅ | ~~없음~~ **정정(2026-08-25)**: 좌 긍정(#dce7fa)/우 부정(#fde4d0) 2열에 [카테고리]+인용문+→인사이트까지 이미 구현돼 있었다(`workspace.ts`) |
| L27 | 핵심구매요소별 평균 만족도 표 | `purchaseFactorSatisfactionBlock` | ✅ | **구현(2026-08-25)**: 투블럭 원본 19쪽 실측. 전치 표라 `tableBlock`에 `labelColumn`·`npsBands` 두 옵션만 더했다. 색은 최고·최저가 아니라 **NPS 치환 구간**(9~10 파랑 / 8~7 흰색 / 0~6 주황) |

### 헤더 · 텍스트 · 기타

| 코드 | 이름 | 현재 구현 | 판정 | 실측 내용 |
|---|---|---|---|---|
| L10 / L10a / L10b | 장·절·문항 헤더 | `headingBlock` | ✅ | **해결(2026-08-25)**: 문항 텍스트는 원래도 raw data 헤더에서 왔고(리바랩스 헤더가 요약 라벨일 뿐), 빠져 있던 건 **척도 주석**이었다. `scaleNoteFromQuestion()`이 문항 원문의 `[A / B]`에서 `* A: 0점 / B:10점`을 도출해 문항 아래 오른쪽에 붙인다. Ⅲ장은 헤더가 전체 문항이면 그대로 쓰고 아니면 표준 문장을 만든다. **"문항 번호가 어긋난다(Q18↔Q21)"는 오진이었다 — 아래 참고** |
| L11 | 정성 서술 블록 | HTML 문자열 | ⚠️ | 설계상 HTML이 맞음. 단 리바랩스 형식이 문자열에 박혀 있음 |
| L11a | 극성 총평 박스 | `responseSummaryHtml` | ⚠️ | 게다가 생성이 opt-in(PRD 11장 15번). 원본은 문항마다 필수 |
| L11b | 개선 아이디어 2단 | `improvementCategoryHtml` | ⚠️ | |
| L24 | 우선순위 종합 해석 | `sectionAnalysisPanelHtml` | ⚠️ | |
| L26 | 조사 결과 서술 박스 | `valueSummaryBoxHtml` | ✅ | ~~없음~~ **정정(2026-08-25)**: 6장 1절(가치별 `[ ○○ 조사 결과 ]`)과 2절(장 종합 해석) 둘 다 이미 있었다 |
| L15 | 페이지 푸터 | `PageFooter` | ✅ | **해결(2026-08-25)**: `ReportDocumentChrome.tsx`의 `PageFooter`를 본문 A4 쪽마다 붙였다. 점선 규칙 + 좌 `{연도} by {브랜드}`(8pt) + 정중앙 `- N -`(11.5pt) + 우 워드마크. 표지(1)·목차(2) 다음부터 번호가 이어지고 판권면에는 없다 |
| L28 | 표지 | `ReportCoverPage` | ✅ | 원본과 대조 완료 |
| L29 | 목차 | `ReportTocPage` | ✅ | 원본과 대조 완료 |
| L30 | 판권면 | `ReportBackCoverPage` | ✅ | 원본과 대조 완료 |

---

## 2. 코드 단위가 아닌 구조 문제 (더 중요)

개별 레이아웃보다 이쪽이 원본과의 거리가 크다.

### 2-1. ~~Ⅲ장 "문항 요약 페이지"라는 골격이 없다~~ → 골격은 있었다. 정성에 묶여 있던 것이 문제 (해결)

원본 8쪽은 문항 하나가 **2×2 표 한 장**으로 완결된다.

```
Q6. "펫과의 산책" 기능의 만족도는 몇 점입니까?        ← L10b
┌─────────────────────────┬───────────────┐
│ 만족도 점수 평균 : 6.35 / 10   │ 표준편차 : 2.54  │   ← L9
├─────────────────────────┼───────────────┤
│ 만족도 분포도 (L1b)          │ 주요 키워드 도출 (L12) │
├─────────────────────────┼───────────────┤
│ 주관식 응답 감정 분석 (L8 반원도넛) │ 응답 요약 (L11a)   │
│ 긍정·부정·중립 % 표 (L8t)      │ [긍정 의견 요약] …   │
└─────────────────────────┴───────────────┘
```

**정정(2026-08-25)**: 이 골격은 `featureQualitativeBlocks()`에 원본 그대로 이미 구현돼 있었다.
데모 화면에서 안 보였던 진짜 이유는 **정성 결과가 없는 문항을 통째로 건너뛰었기 때문**이다
(`if (q.categories.length === 0) continue`). 원본 페이지의 상단 절반(평균·표준편차 배너,
만족도 분포도, 키워드 칸)은 raw data만으로 그려지는 정량 블록인데, 정성 분석 전이거나 한 문항이
실패하면 그 쪽이 통째로 사라져 보고서 구조 자체가 달라졌다.

**해결**: 순회 기준을 정성 문항 → **정량 기능**으로 뒤집었다. 페이지의 주인은 정량이고 정성은
있으면 얹는다. 정성이 없는 문항은 하단 절반만 "정성 분석 승인 후 표시"로 남는다.
정성 분석 실패가 정량 결과 출력까지 막지 않는다는 원칙(PRD 11장 15번)과 같은 방향이다.
렌더 확인: 기능 6개 문항 페이지가 모두 생성되고 Q번호·평균·표준편차가 원본과 일치한다.

### 2-2. ~~장 안의 순서가 원본과 반대다~~ → 오진이었다

**정정(2026-08-25)**: `buildFeatureSection()`은 문항별 페이지를 먼저, 종합 차트·순위표를
나중에 낸다 — 원본·레이아웃 PDF와 같은 순서다. 데모에서 문항별 페이지가 대기 문구 한 줄로
축약돼 있어 종합 차트가 먼저 오는 것처럼 보였을 뿐이다. 2-1을 고치면서 함께 해소됐다.

### 2-3. 쪽이 비어 있다

블록 하나만 든 A4 쪽이 여러 장 나온다(예: 작은 "정성 분석 대기" 박스 하나로 한 쪽). PRD
3.3.3절(A4 쪽 편집 웹뷰)에서 다룰 문제지만, **레이아웃을 원본과 같게 만들어도 쪽 채우기가
안 되면 "원본과 같은 보고서"가 되지 않는다** — 두 작업은 같이 가야 한다.

### 2-4. ~~화면 UI가 인쇄물에 섞인다~~ (해결)

**해결(2026-08-25)**: 인쇄 규칙에 `[data-copy-ignore]:not([data-print-keep]) { display:none }`
한 줄을 넣었다. 다만 **복사 제외와 인쇄 제외는 다른 문제**라, 페이지 푸터와 담당자가 붙인
이미지는 `data-print-keep`으로 되살리고 그 안의 첨부·삭제 버튼만 다시 숨긴다(그냥 전부
숨기면 첨부한 이미지까지 인쇄에서 사라진다). `EditableBarChart`의 툴바에만 `data-copy-ignore`가
빠져 있어 PNG 버튼이 한글 복사·인쇄에 실리던 것도 같이 고쳤다.
렌더 확인: 출력 PDF 40쪽에서 화면 전용 문구 0건.

---

## 3. 작업 순서 제안

| 순위 | 대상 | 근거 |
|---|---|---|
| ~~1~~ | ~~Ⅲ장 문항 요약 페이지 골격(2-1)~~ | **완료(2026-08-25)** — 골격은 있었고, 정성 유무에 묶여 있던 것을 끊었다 |
| ~~2~~ | ~~L10b 문항 헤더를 raw data 원문으로~~ | **완료(2026-08-25)** — 척도 주석 도출 + Ⅲ장 원문 우선. 문항 번호는 고치지 않았다(아래 4절 "오진" 참고) |
| ~~3~~ | ~~L6 사분면, L1a 축 규칙~~ | **완료(2026-08-25)** |
| ~~4~~ | ~~L15 푸터, L9 표 서식~~ | **완료(2026-08-25)** — 아래 4절 참고 |
| ~~5~~ | ~~L13·L13a·L14·L19·L20·L25·L26·L27 신규~~ | **8종 중 6종 완료(2026-08-25)** — 3종은 이미 있었고(L25·L26), 1종은 기존 블록으로 충분했으며(L19), 3종을 새로 만들었다(L13·L13a·L27). **남은 2종은 L14·L20** |
| ~~6~~ | ~~인쇄 시 UI 숨김(2-4)~~ | **완료(2026-08-25)** |

1~4는 리바랩스 원본 재현 작업이고, 5가 범용화 작업이다. **1~4를 먼저 끝내야 5를 만들 때
"원본과 같다"의 기준이 생긴다.**
---

## 4. 다음 세션 시작점 (2026-08-25 인계)

### 지금 어디까지 왔나

- **단계 A(레이아웃 원본 재현) 진행 중.** 전체 순서는 A 레이아웃 → B 에이전트 → C 범용화 → D A4 쪽 편집(PRD 2.2.2·3.3.3).
- **3절 목록 1~6번 전부 완료.** 리바랩스 원본 재현(1~4)과 인쇄 UI 숨김(6)이 끝났고,
  범용 신규 레이아웃(5)은 8종 중 6종이 정리됐다 — **남은 것은 L14·L20 두 종뿐**이며,
  둘 다 렌더러보다 **입력 데이터 경로**가 먼저 필요하다(아래 참고).
- 워터폴(L13a) "증감 0인 구간은 막대 없음"은 코드와 `STAGE_MAPPING.xlsx` 레이아웃
  시트 양쪽에 반영 완료(2026-08-25). **Notion은 폐기됐고 이 엑셀이 유일한 원본이다.**
- 마지막 검증: `npm run check:golden` 93/93, `check:question-header` 14/14,
  `check:layout-blocks` 15/15 PASS, typecheck·lint·`next build` 통과,
  `/viewer` 헤드리스 PDF 40쪽 렌더로 원본(리바랩스 3·4·5·8·37쪽, 케어클 34쪽, 투블럭 19쪽) 대조.

### 4번(L15 푸터 + 표 서식 토큰)에서 실제로 한 일 (2026-08-25)

1. **L15 푸터** — `PageFooter`(`ReportDocumentChrome.tsx`)를 본문 `<section>`마다 붙였다.
   위치·크기는 원본 3쪽 150dpi 픽셀 실측: 점선 규칙 y=277.4mm(x 15.4~195.3mm), 좌측 텍스트
   x=17.4mm, 워드마크 글자폭 32.8mm. 쪽번호는 표지=1·목차=2 다음부터 이어진다.
2. **표 서식 토큰** — `sectionStyle.ts`에 `dataTableCss(paletteIndex)`를 추가하고
   `workspaceOverview`·`workspaceNps`·`workspace`·`workspaceConclusion`의 HTML 문자열 표와
   `ReportBlockView`의 React 표(제품·서비스 개요, row-group)에 적용했다. 원본 3·5·8쪽을 직접
   샘플링해 확인한 값: 테두리 **#6182d6**(회색 #d4d4d8이 아니다), 제목행 **#c0cdef**,
   머리글·라벨 **#dfe6f7**. 설문 항목 표의 단계 칸과 "총 N 문항" 행도 #dfe6f7이 맞다.
3. **`.rivalabs-*` CSS(globals.css)** 는 토큰을 읽을 수 없는 별도 표현 계층이라 값만 같게
   맞췄다(#6b87d6→#6182d6, #bdcbed·#d6e0f4→#c0cdef). 팔레트를 바꾸면 여기도 같이 고칠 것.

### 이때 같이 잡은 두 버그 (푸터를 붙이지 않았으면 안 보였을 것들)

- **쪽 묶기(pageGroups)가 블록 높이를 계속 적게 셌다.** ① 블록 사이 간격(안쪽 요소의
  margin-bottom)이 margin collapsing으로 `getBoundingClientRect`에 안 잡혀 고정 8px로
  어림하고 있었고(실제 15~20px), ② 이미지 첨부 슬롯처럼 첫 레이아웃 뒤에 커지는 블록이
  있는데 측정이 최초 1회뿐이었다. 그 결과 Ⅰ장 첫 쪽 `<section>`이 **322mm**가 되어 인쇄에서
  브라우저가 다시 쪼갰고, 푸터가 다음 쪽으로 밀렸다. 실제 margin 값을 읽어 더하고
  `ResizeObserver`로 다시 재게 고쳤다(37쪽 중 2쪽만 초과 — 둘 다 한 블록이 한 쪽보다 큰
  경우로, 3절 2-3 "쪽 채우기"에서 다룰 문제다).
- **`globals.css`의 인쇄 규칙이 `padding: 18mm !important`로 쪽 여백을 덮어쓰고 있었다.**
  본문 `<section>`의 pb, 인쇄 규칙의 padding, `pageContentHeight` 계산 **세 곳이 항상 같은
  값이어야 한다** — 하나만 고치면 위 증상이 그대로 재발한다.
- (부수) 결과요약 기능 표에서 정성 분석 전 극성 비율이 전부 0일 때 "최댓값 강조"가 열 전체를
  칠하던 버그도 `max > 0` 가드로 막았다(PDF 렌더러가 이미 겪었던 것과 같은 버그).

### 2번(L10b 문항 헤더)에서 실제로 한 일 (2026-08-25)

원본 37쪽을 150dpi로 다시 읽어보니 L10b의 실제 구조는 이랬다:

```
Q18. 캣독런의 조작은 [불편하다 / 편하다]
                              * 불편하다: 0점 / 편하다:10점   ← 우측 정렬, 9pt
────────────────────────────────────────  ← 시안 밑줄 #42c7f1, 2.9pt, 15.4~192.4mm
```

- **척도 주석이 진짜 빠져 있던 것**이었다. 문구를 따로 입력받지 않고 `scaleNoteFromQuestion()`
  (`lib/report/sections.ts`)이 **문항 원문의 `[A / B]`에서 도출**한다 — 척도의 양 끝은 문항에
  이미 적혀 있으므로 다른 raw data에도 그대로 통한다. 대괄호가 없는 헤더(요약 라벨·인적사항)는
  주석을 안 단다(없는 척도를 지어내지 않는다). `headingBlock()` 안에서 도출하므로 **모든 장의
  문항 헤더가 한 번에** 얻는다.
- **문항 텍스트는 원래도 raw data에서 왔다.** Ⅱ·Ⅴ·Ⅵ·Ⅷ장은 `stats.surveyQuestions`를 그대로
  쓰고 있었고, 리바랩스 헤더가 요약 라벨이라 라벨로 보였을 뿐이다(전체 문항을 넣은 raw data는
  그대로 나온다 — PRD 5.1절 검증 완화). 다만 **Ⅲ장만 항상 표준 문장을 만들고 헤더를 무시**하고
  있어서, `isFullQuestionText()`(물음표 또는 척도 대괄호가 있으면 원문)로 원문을 우선하게 고쳤다.
- Ⅵ장 소절 상단의 "* 0점 ~ 10점 척도" 한 줄은 원본에 없다 — 문항별 주석을 뽑을 수 있으면 빼고,
  못 뽑는 raw data(리바랩스)에서만 남긴다.
- 검증: `npm run check:question-header`(순수 규칙 검사 14케이스, LLM·DB 없음) + 브라우저에서
  문항을 실제로 `[불편하다 / 편하다]`로 고쳐 주석이 다시 도출되는지 라이브 확인.

#### 주의 — "문항 번호가 어긋난다(Q18 ↔ Q21)"는 오진이었다

원본 **본문 37쪽은 Q18**, 그런데 **원본 설문 항목 표(5쪽)는 같은 문항을 Q27**로 적고 있다.
원본 자체가 내부적으로 어긋나 있다(CLAUDE.md에 기록된 "본문 Q13 ↔ 표 Q16"과 같은 사례).
본문 번호는 유사 서비스 문항 3개를 안 세고, 표 번호는 핵심구매요소를 7문항으로 쪼개 센 결과다.
**우리 번호(Q21)는 우리 설문 항목 표와 일관되며 raw data 컬럼에서 결정론적으로 나온다** —
어느 한쪽 원본 숫자에 맞추려고 코드를 고치지 말 것(CLAUDE.md "골든 테스트셋" 절의 원칙과 같다).

### 5·6번에서 실제로 한 일 (2026-08-25)

**6번(인쇄 시 UI 숨김)** — 인쇄 규칙에 `[data-copy-ignore]:not([data-print-keep]){display:none}`
한 줄. **복사 제외와 인쇄 제외는 다른 문제**라 푸터·첨부 이미지는 `data-print-keep`으로
되살리고 그 안의 버튼만 다시 숨긴다(전부 숨기면 담당자가 붙인 이미지까지 사라진다).
출력 PDF 40쪽에서 화면 전용 문구 0건 확인.

**5번(범용 신규 레이아웃)** — 8종 중 3종은 이미 있었고, 1종은 기존 블록으로 충분했다.
새로 만든 것은 3종이다.

- **L13 시점 추이 꺾은선 · L13a 만족도 변화 워터폴**(`EditableJourneyCharts.tsx`):
  케어클 원본 34쪽 픽셀 실측(선 #667eea, 배경 #eff1fd, 상승 #71bf73, 하락 #f6695f/#ffad33).
  **워터폴 구간은 꺾은선의 시점 목록에서 계산한다** — 호출부가 증감을 따로 넘기면 나란히 놓인
  두 그래프가 어긋날 수 있어서다. 우리가 계산한 증감(-1.92 / +0.04 / +0.36 / -0.32)이 원본에
  인쇄된 값과 정확히 일치하는 것을 `check:layout-blocks`로 고정했다.
  - **증감이 0인 구간은 막대를 그리지 않는다**(2026-08-25 담당자 확인). 원본은 시점이 6개인데
    막대가 4개다 — 첫인상(9.08)→개봉(9.08) 구간이 변화가 없어 빠졌고, 같은 쪽 위의 표에서도
    그 칸만 증감이 `(-)`로 찍혀 있다. 처음엔 카탈로그의 "시점이 N개면 막대는 N-1개"를 따라
    5개로 만들었다가 원본 기준으로 되돌렸다. **`STAGE_MAPPING.xlsx` 레이아웃 시트의
    L13a 줄도 같이 고쳤다**(2026-08-25 — 시트가 단일 출처이므로 코드와 어긋난 채로 두지 않는다).
    다만 모든 구간이 0이면 그래프가 통째로 비므로 그때만 거르지 않는다.
- **L27 핵심구매요소별 평균 만족도 표**: 전치 표라 새 블록처럼 보이지만 데이터 모양은
  `tableBlock`과 같다. `labelColumn`·`npsBands` 두 옵션만 더했다. 색은 최고·최저 강조가 아니라
  **NPS 치환 구간**(9~10 파랑 / 8~7 흰색 / 0~6 주황)이다 — 투블럭 원본 19쪽에서 8.27이
  파랑이 아니라 **흰색**인 것을 픽셀로 확인해 범례 그대로임을 확정했다(60dpi로는 파랑으로 보여
  하마터면 기준을 잘못 정할 뻔했다). 범례의 `■`는 서브셋 폰트에 없어 CSS 사각형으로 그린다.
- **정정 — L25·L26은 이미 구현돼 있었다**: 4대 가치의 긍정·부정 좌우 2열 표는
  `valueOpinionColumnHtml`, `[ ○○ 가치 조사 결과 ]` 서술 박스는 `valueSummaryBoxHtml`이다.
  **L19도 새 렌더러가 필요 없다** — 카테고리별 세로 막대(명)라 기존 `chartBlock`이 그대로 맞고,
  선행 조건은 렌더러가 아니라 자유서술 카테고리화(파이프라인)다.

#### 남은 2종은 렌더러보다 "입력 경로"가 먼저다

- **L14 경쟁재 비교표**: ~~장단점 분리 컬럼이 있는 raw data가 없다~~ → **2026-08-26 정정.
  담당자 지적으로 raw data를 직접 열어보니 이젠오토(26~29번)·정리습관(34~38번)에 서비스명·
  장점·단점이 별도 컬럼으로 다 있었다.** 카탈로그의 "이젠오토·정리습관만 분리 수집"이라는
  메모가 바로 그 뜻이었는데, 나는 그걸 "우리한테 그런 데이터가 없다"로 잘못 읽었다.
  **원본 PDF와 카탈로그만 보고 raw data를 안 열어본 것이 원인이다 — 데이터 유무를 말하기
  전에 반드시 raw data를 먼저 열 것.**
  - 남은 진짜 과제는 렌더러가 아니라 **정성 처리**다. 이젠오토 원본 8쪽 표의 행 이름
    ("마이클", "기타 출장 서비스")과 짧은 불릿은 45명의 자유서술을 **서비스별로 묶고 요약한
    결과**다. raw 응답은 "집으로 찾아와서 배터리 교체했던 서비스", "지방에서 엔진오일을
    교체하였음"처럼 제각각이라 그대로 넣으면 45행짜리 전혀 다른 표가 된다.
  - 즉 L14 = rich-static 표 렌더러 + (서비스명 클러스터링 + 장·단점 요약) 정성 단계.
- **L20 경험 수준 정의표**(이젠오토 7쪽): 좌측 세로 병합 라벨 + 단계명 + 설명문 3열.
  문제는 **설명문("와이퍼, 워셔액, 전구 등 간단한 소모품을 교체한 경험이 있음")이 raw data
  응답값에 없다**는 것이다 — 설문 설계 정보라 담당자 입력이든 설문지 첨부든 별도 경로가
  필요하다. **이건 렌더러 과제가 아니라 입력 스펙 과제이므로 임의로 만들지 않았다.**

둘 다 역할 분류 에이전트(PRD 2.2.2절 2단계)가 그 컬럼을 판정할 수 있게 된 뒤에 붙이는 것이
순서상 맞다 — 지금 만들면 넣을 데이터도, 맞는지 확인할 방법도 없다.

### 원본과 대조하는 방법 (이 루프를 그대로 쓸 것)

코드 리뷰로는 이 종류의 차이를 못 잡는다. **반드시 렌더해서 원본과 나란히 본다.**

```bash
# 1) 웹 작업공간을 통째로 PDF로 출력 (데모 = 리바랩스 정량, DB 불필요)
npm run dev &
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --disable-gpu \
  --no-pdf-header-footer --print-to-pdf=/tmp/web.pdf --virtual-time-budget=25000 \
  "http://localhost:3000/viewer"

# 2) 찾는 페이지 번호 알아내기
pdftotext -layout /tmp/web.pdf /tmp/web.txt   # 텍스트에서 장 제목 검색

# 3) 우리 결과 / 원본을 같은 배율로 이미지화해 눈으로 대조
pdftoppm -jpeg -r 60 -f <쪽> -l <쪽> /tmp/web.pdf /tmp/web
pdftoppm -jpeg -r 60 -f <쪽> -l <쪽> "data/[알파브라더스] 리바랩스_..._0904_상연.pdf" /tmp/orig
```

색·크기를 눈대중으로 정하지 말 것. 원본에서 직접 뽑는다:

```bash
# 색: 원본 페이지를 150dpi로 렌더해 픽셀에서 샘플링
pdftoppm -png -r 150 -f 37 -l 37 "data/[알파브라더스] 리바랩스_..._0904_상연.pdf" /tmp/o
python3 -c "from PIL import Image;from collections import Counter;\
print(Counter(Image.open('/tmp/o-37.png').convert('RGB').getdata()).most_common(12))"

# 글자 크기: 글리프 bbox 높이를 장 제목(15pt로 알려짐)으로 보정 → ÷1.107
pdftotext -f 37 -l 37 -bbox "data/[알파브라더스] 리바랩스_..._0904_상연.pdf" /tmp/p37.html
```

### 주의 — 이번 세션에서 세 번 틀린 지점

렌더 한 장면만 보고 "이 레이아웃은 없다/다르다"고 단정했다가 세 번 정정했다(2절 참고).
**판정 전에 반드시 (a) 해당 빌더 코드를 열어보고 (b) 원본 PDF의 실제 쪽을 확인할 것.**
데모에는 정성 분석 결과가 없어서, 정성이 붙는 블록은 "대기" 상태로만 보인다 — 그것을
"구현이 없다"로 읽으면 안 된다.
